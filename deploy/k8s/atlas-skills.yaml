---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-skills
  namespace: atlas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-skills
  template:
    metadata:
      labels:
        app: atlas-skills
    spec:
      containers:
      - name: skills
        image: ghcr.io/chronicle20/atlas-skills/atlas-skills:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-skills"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-skills
  namespace: atlas
spec:
  selector:
    app: atlas-skills
  ports:
  - protocol: TCP
    port: 8080
