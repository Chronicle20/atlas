---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-chairs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-chairs
  template:
    metadata:
      labels:
        app: atlas-chairs
    spec:
      containers:
      - name: chairs
        image: ghcr.io/chronicle20/atlas-chairs/atlas-chairs:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: SERVICE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['app']
        - name: LOG_LEVEL
          value: "debug"
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-chairs
spec:
  selector:
    app: atlas-chairs
  ports:
  - protocol: TCP
    port: 8080