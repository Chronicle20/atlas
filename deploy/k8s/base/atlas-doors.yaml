---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-doors
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-doors
  template:
    metadata:
      labels:
        app: atlas-doors
    spec:
      containers:
      - name: doors
        image: ghcr.io/chronicle20/atlas-doors/atlas-doors:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "info"
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-doors
spec:
  selector:
    app: atlas-doors
  ports:
  - protocol: TCP
    port: 8080
