apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-maker
  labels:
    atlas.seed-catalog: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-maker
  template:
    metadata:
      labels:
        app: atlas-maker
    spec:
      volumes: []
      containers:
      - name: maker
        image: ghcr.io/chronicle20/atlas-maker/atlas-maker:latest
        volumeMounts: []
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: SERVICE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['app']
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-maker"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-maker
spec:
  selector:
    app: atlas-maker
  ports:
  - protocol: TCP
    port: 8080
