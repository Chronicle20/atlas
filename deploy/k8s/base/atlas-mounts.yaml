---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-mounts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-mounts
  template:
    metadata:
      labels:
        app: atlas-mounts
    spec:
      containers:
      - name: mounts
        image: ghcr.io/chronicle20/atlas-mounts/atlas-mounts:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-mounts"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-mounts
spec:
  selector:
    app: atlas-mounts
  ports:
  - protocol: TCP
    port: 8080
