---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-rates
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-rates
  template:
    metadata:
      labels:
        app: atlas-rates
    spec:
      containers:
      - name: rates
        image: ghcr.io/chronicle20/atlas-rates/atlas-rates:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: SERVICE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['app']
        - name: LOG_LEVEL
          value: "debug"
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-rates
spec:
  selector:
    app: atlas-rates
  ports:
  - protocol: TCP
    port: 8080
