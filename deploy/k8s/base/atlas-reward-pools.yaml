---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-reward-pools
  labels:
    atlas.seed-catalog: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-reward-pools
  template:
    metadata:
      labels:
        app: atlas-reward-pools
    spec:
      volumes: []
      containers:
      - name: gachapons
        image: ghcr.io/chronicle20/atlas-reward-pools/atlas-reward-pools:latest
        volumeMounts: []
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-reward-pools"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-reward-pools
spec:
  selector:
    app: atlas-reward-pools
  ports:
  - protocol: TCP
    port: 8080
