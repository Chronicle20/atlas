---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-rps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-rps
  template:
    metadata:
      labels:
        app: atlas-rps
    spec:
      containers:
      - name: rps
        image: ghcr.io/chronicle20/atlas-rps/atlas-rps:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-rps
spec:
  selector:
    app: atlas-rps
  ports:
  - protocol: TCP
    port: 8080
