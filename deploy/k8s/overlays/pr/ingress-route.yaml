apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: atlas-pr-ingress
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`PLACEHOLDER_PR_NUMBER.atlas.home`)
      kind: Rule
      services:
        - name: atlas-ingress
          port: 80
