# dispatcher-lint baseline — families NOT yet migrated to the canonical
# discrete-per-mode pattern described in docs/packets/DISPATCHER_FAMILY.md.
#
# `packet-audit dispatcher-lint` SKIPS violations whose dispatcher family
# (the base IDA FName of the `#`-suffixed candidatesFromFName entries, e.g.
# `CWvsContext::OnPartyResult`) appears below, and prints one note line per
# exempt family instead.
#
# CAMPAIGN COMPLETE (task-105): the list is now EMPTY. Every mode-prefix
# dispatcher family — guild (task-103), message (task-104), and finally party
# (CWvsContext::OnPartyResult) + buddy (CWvsContext::OnFriendResult) (task-105)
# — has been migrated to the canonical discrete-per-mode pattern: one struct per
# enumerated mode, fixed-key per-mode body funcs, config-resolved mode bytes,
# per-mode byte fixtures + pinned evidence. No caller-specified operation
# selectors remain (PartyErrorBody / BuddyErrorBody / GuildErrorBody all gone).
#
# THE LIST ONLY SHRINKS. It has reached empty — there is nothing left to migrate.
# Never add a new family to dodge a violation from NEW work: new dispatcher
# families must be authored discrete-per-mode from the start (DISPATCHER_FAMILY.md).
exempt_families: []
