packet: character/serverbound/KeyMapChange
direction: serverbound
version: gms_v83
category: TRUNCATION
ida:
    function: CFuncKeyMappedMan::SaveFuncKeyMap
    address: "0x58df2f"
    decompile_sha256: b91c1bf524df3d529ed8523173de225993373e5867f230acb63f7f1fb634157e
notes: "TRUNCATION (VERIFIED-EXCEPTION, task-109). v83 CFuncKeyMappedMan::SaveFuncKeyMap@0x58df2f: COutPacket(0x87), Encode4(mode=0), Encode4(count), then per changed-key entry Encode4(keyIdx)+EncodeBuffer(5 bytes = nType[1]+nID[4]) via sub_4E43FA(EncodeBuffer,5). Wire is byte-identical to the Atlas codec (mode int32 + count int32 + per-entry [KeyId int32 + TheType int8 + Action int32] = 9 bytes/entry). The report FlatInvalid:true verdicts [0,0,2,2,2,2] are the documented loop-vs-flattened static-diff artifact (same family as jms #47, loop-89-vs-90): the export models a single flattened entry while the codec emits a variable-length loop. NOT a real wire delta — codec unchanged."
verifies:
    - libs/atlas-packet/character/serverbound/key_map_change_test.go#TestKeyMapChangeMode0RoundTrip
