# discover-ops worklist — gms_v61

Generated by `packet-audit discover-ops --version gms_v61` (clientbound switch-walk)
+ a global `COutPacket`-ctor harvest for serverbound. Run against the
**GMS_v61.1_U_DEVM.exe** IDB (instance port **13338**, re-confirmed via
`list_instances`; `active`). There is **no usable v61 CSV column** (ClientBound
values are the placeholder `0x000`; no serverbound v61 column), so the registry
was **not** seeded from CSV. Authority for every in-scope op is
`docs/tasks/task-113-gms-legacy-versions/v61-packet-delta.md`; the IDB switch /
send-site wins on any disagreement. Anchor = `gms_v72.yaml` (FName→Op dictionary);
v61 opcodes were **never copied** — they are non-uniformly shifted.

## Clientbound — method

Built by `discover-ops` switch-walking the v61 dispatchers and joining each
discovered `(opcode, FName)` to the `gms_v72.yaml` FName→Op dictionary. Manual
entries are hand-decompiled cases the numeric switch-parser cannot reach
(char-literal / if-else / vtable / sibling-switch / dialog-region routing).

### Dispatcher set (curation rule §1.1 — INCLUDE top-level-opcode `*::OnPacket`; EXCLUDE body-mode demuxers)

| Address | Function | Form / cases |
|---|---|---|
| `0x47440a` | `CClientSocket::ProcessPacket` | shim; cases 0x10-0x15 (5; 0x14=CSecurityClient not captured) |
| `0x565668` | `CLogin::OnPacket` | switch; login 0-22 (15; op 9 else-if, char-mgmt 13/14/15 rotated symbols) |
| `0x8303eb` | `CWvsContext::OnPacket` | switch; ops 26-91 (60; v72 81-100 block ABSENT) |
| `0x659f99` | `CStage::OnPacket` | char-literals `\`/`]`/`^`=92-94 → 0 numeric cases (manual) |
| `0x4e9ea3` | `CField::OnPacket` | base char-literal switch 97-119 + if-chain routing pools/dialogs (5 numeric high-region) |
| `0x7bd7f3` | `CUserPool::OnPacket` | if/else 120-121 + range-routes → 0 cases (manual) |
| `0x7bdb3e` | `CUserPool::OnUserCommonPacket` | char-literal switch 122-126 + pet 127-133 + summon 134-139 → 0 cases (manual) |
| `0x7bdbda` | `CUserPool::OnUserRemotePacket` | **two sibling switches** 141-159 (parser caught 1st only, 11) |
| `0x7a451a` | `CUserLocal::OnPacket` | switch 160-173 (12) |
| `0x5d4894` | `CMobPool::OnPacket` | switch 175-177,188 (4) |
| `0x5d48f3` | `CMobPool::OnMobPacket` | switch 178-192 (12) |
| `0x5efbad` | `CNpcPool::OnPacket` | switch 78(dead),194-200 — **held out of --apply** to clear the 78 internal-collision gate (manual) |
| `0x4d3450` | `CEmployeePool::OnPacket` | switch 202-204 (3) |
| `0x4c9163` | `CDropPool::OnPacket` | if/else 205-206 → 0 cases (manual) |
| `0x5bc188` | `CMessageBoxPool::OnPacket` | switch 207-209 (3) |
| `0x423eb7` | `CAffectedAreaPool::OnPacket` | if/else 210-211 → 0 cases (manual) |
| `0x68745a` | `CTownPortalPool::OnPacket` | if/else 212-213 → 0 cases (manual) |
| `0x633133` | `CReactorPool::OnPacket` | switch 214,216,217 (3; 215 REACTOR_MOVE absent) |
| `0x4610a4` | `CCashShop::OnPacket` | switch 253-260 (7; clean Δ-36 shift of v72 289-296) |

Additional manual dispatchers decompiled for the char-literal / if-routed families:
`CUser::OnPetPacket` @0x79225f (pet 127-133), `sub_7922E8` @0x7922e8 (summon
134-139), `CNpcPool::OnNpcPacket` @0x5efd04 (npc leaf 197-199), `CStage::OnSetField/ITC/CashShop`,
`CFuncKeyMappedMan::OnPacket` @0x51ab57 (keymap 262-264), `sub_59BD51` (TV 268-270),
`sub_5A81B9` (low ops 95-96), and the CField high-region dialog routes 235-244/252.

### Clientbound totals

- **220 clientbound** entries (140 ida-discovered via `--apply` + 80 manual).
- No unresolved Missing; the only discovery-internal collision (0x4E dead-case) was
  adjudicated (below). YAML self-validates; no duplicate opcodes within direction.

### Clientbound adjudications

1. **0x4E (op 78) discovery-internal collision — RESOLVED (CWvsContext wins).**
   `CClientSocket::ProcessPacket` routes the [0x1A,0x5B] window to
   `CWvsContext::OnPacket`, whose case 78 → `sub_830B0B` (OnImitatedNPCData).
   `CNpcPool::OnPacket` (0x5efbad) carries a **dead** 78 case
   (`OnNpcImitateData`) never reached (CField routes only 194-201 to CNpcPool).
   Same dead-case family as v72. Registered at the CWvsContext handler with the
   CNpcPool name as `fname_alt`. The CNpcPool dispatcher was held out of the
   `--apply` union pass to clear the gate; its base ops 194-200 are registered manual.
2. **Char-management 13/14/15 ROTATED SYMBOLS** — the v61 IDB labels on
   `CLogin::OnPacket` cases 13/14/15 are rotated one step off their handler bodies
   (identical to the v72/v79 trap). Registered by the **body-canonical (v83) FName**
   (mirroring the corrected `gms_v72.yaml`): 13=CHAR_NAME_RESPONSE, 14=ADD_NEW_CHAR_ENTRY,
   15=DELETE_CHAR_RESPONSE — verified by decompiled read-order, not the symbol label.
3. **SELECT_CHARACTER_BY_VAC (op 9)** — `CLogin::OnPacket` else-if branch (not a
   switch case). Manual.
4. **CWvsContext 81-100 block ABSENT** — v61 CWvsContext ends at op 91
   (MACRO_SYS_DATA_INIT); the v72 HourChanged→NotifyWedding/Family block (19 ops) is
   absent. The tail 82-91 maps to v72 101-113 by FName.
5. **CField base region compressed** — v61 lacks FIELD_OBSTACLE_ONOFF (single),
   FIELD_OBSTACLE_ALL_RESET, PLAY_JUKEBOX (v72 127/129/131). op 106 = BLOW_WEATHER
   (sub_4ED39C, body Decode4(itemId)+GetItemString+DecodeStr+CHATLOG_ADD).
6. **CUserRemote / CUserLocal shrinkage** — v61 lacks SHOW_UPGRADE_TOMB_EFFECT
   (remote), and MakerResult/KOREAN_EVENT/LOCK_UI/DISABLE_UI/SPAWN_GUIDE (local),
   matching the delta doc. No CHATTEXT1 (single OnChat at op 122).
7. **v61-EXTRA op 21 (0x15)** — ProcessPacket case 0x15 → sub_4747E2 (Decode2 count +
   COutPacket(27) reply); no v72 equivalent, op-name deferred to Stage E.
8. **Dialog-region demuxer ops (239/243/244)** carry provisional op-names
   (ADMIN_SHOP / PLAYER_INTERACTION / MESSENGER) from the v72 structural region;
   their top-level opcode is verified from `CField::OnPacket` routing, but exact
   family/mode = Stage E. 242=STORAGE and 252=RPS_GAME are FName-confirmed.

## Serverbound — method

Read from the v61 `COutPacket(N)` send-sites via a **global COutPacket-ctor harvest**
(ctor `??0COutPacket@@QAE@J@Z` @ `0x5ffc4f`): every code xref to the ctor was walked,
the nearest preceding `push imm` taken as the opcode, and each caller function's
name matched to the `gms_v72.yaml` serverbound FName. **Opcodes were never
region-shift-predicted** — each is the literal COutPacket argument.

The five delta-doc anchors reproduced exactly: LOGIN_PASSWORD=1 (Δ0),
CHANGE_MAP=35 / CHANGE_CHANNEL=36 / MOVE_PLAYER=38 (Δ-2), PARTY_OPERATION=112 /
DENY_PARTY_REQUEST=113 (Δ-10). The shift is **non-uniform**: login≈Δ0 (DELETE_CHAR
drops to the v83-style 23, so PONG/CLIENT_START_ERROR are Δ-1); map/attack region
Δ-2; social/note/keymap region Δ-10; pet/npc/reactor region Δ-19..-25; the
CWvsContext item/ability cluster is Δ0.

### Serverbound totals

- **69 serverbound** ops send-site-verified and registered (`provenance: ida-discovered`,
  `ida.address` = the COutPacket-ctor harvest anchor; the per-op note names the v61
  sender and the COutPacket(N) literal).

### Serverbound COMPLETION (the 43 remaining — RESOLVED Stage B2)

The 43 remaining ops were resolved by **body-signature matching**, not
name-join or blind-shift. Method: harvested v72's `COutPacket(N)` encode-sequence
per opcode (ground truth from `gms_v72.yaml`'s verified opcodes), harvested v61's
full opcode→(fname, encode-signature) map from the ctor (`0x5ffc4f`), then matched
each v72 op to the v61 opcode whose **body signature** matches — never the symbol
label. This caught the "name trap" repeatedly (v61 op92 = CheckPortal_Collision =
CHANGE_MAP_SPECIAL, NOT SKILL_EFFECT which is op85; v61 op85 body `[E4,E1,E1,E1]`
matches v72 SKILL_EFFECT). Where a v61 function keeps the same demangled name in
both IDBs (CheckPortal_Collision, TryEnterTownPortal, CField_Coconut::BasicActionAttack,
CCashShop::TrySendQueryCashRequest), that confirmed identity directly.

**38 RESOLVED** (v61 opcode ← v72 opcode):
CANCEL_CHAIR=39, USE_CHAIR=40, CLOSE_RANGE_ATTACK=41, MAGIC_ATTACK=43,
TAKE_DAMAGE=45, GENERAL_CHAT=46, CLOSE_CHALKBOARD=47, MONSTER_BOOK_COVER=53,
ITEM_SORT2=69, USE_RETURN_SCROLL=78, USE_UPGRADE_SCROLL=79, MESO_DROP=82,
SKILL_EFFECT=85, CHANGE_MAP_SPECIAL=92, SUE_CHARACTER=104, WHISPER=108,
SPOUSE_CHAT=109, MESSENGER=110, USE_DOOR=121, ADMIN_COMMAND=126, BBS_OPERATION=134,
MOVE_PET=138, PET_COMMAND=140, PET_LOOT=141, PET_EXCLUDE_ITEMS=143,
SUMMON_ATTACK=147, DAMAGE_SUMMON=148, MOVE_LIFE=155, MOB_DROP_PICKUP_REQUEST=157,
FIELD_DAMAGE_MOB=158, MOB_DAMAGE_MOB_FRIENDLY=159, MONSTER_BOMB=160,
MOB_DAMAGE_MOB=161, ITEM_PICKUP=169, COCONUT=178, MONSTER_CARNIVAL=183,
CHECK_CASH=195, CASHSHOP_OPERATION=196. Each entry's `note` quotes the
`COutPacket(N)` + encode body and cites the send-site address.

**3 pre-existing blind-inheritance mislabels CORRECTED** (the earlier Stage-B pass
had assigned these v61 opcodes the *op-name of the v72 opcode with the same number*
— exactly the trap the delta doc warned against; body evidence overturns them):

| existing entry (WRONG) | v61 opcode | true identity (body-verified) | correction |
|---|---|---|---|
| `SKILL_MACRO=109` | 109 | `[Str,Str]` from `CUIStatusBar::OnKey` = couple msg | → **SPOUSE_CHAT=109** |
| `PLAYER_INTERACTION=121` | 121 | `GetPartyTownPortal`+`Encode4(map)+Encode1` = door | → **USE_DOOR=121** |
| `MOVE_SUMMON=169` | 169 | `[E1,E4,E2,E2,E4]` inventory region, no movepath | → **ITEM_PICKUP=169** |

The true opcodes for the displaced ops were then located and **added**:
`PLAYER_INTERACTION=111` (the miniroom/trade/entrusted-shop dispatcher, dozens of
`COutPacket(111)+Encode1(mode)` sub-senders) and `MOVE_SUMMON=146` (`Encode4(summonId)+
CMovePath::Flush`, contiguous with SUMMON_ATTACK 147 / DAMAGE_SUMMON 148, Δ-23).
`SKILL_MACRO`'s true v61 send-site was **not located** (see flagged, below).

**4 DROPPED-ABSENT (IDA evidence):**
- `ADMIN_CHAT` (v72 116) — v72 body is a multi-line `[E1,E1,Str]×N` GM notice; **no
  v61 opcode carries that repeated pattern**. v61 GM chat is the single
  ADMIN_COMMAND=126 packet.
- `ADMIN_LOG` (v72 127) — v61 opcode 127 is `WEDDING_ACTION`; no standalone `[Str]`
  GM-log sender exists in v61.
- `MATCH_TABLE` (v72 204) — v61 opcode 204 = `sub_70AE2E [E2,E4,E4,E4]` (a
  VARIANTARG script-field event), not the v72 `[E1]` match-table; no `[E1]`
  match-table sender found.
- `DENY_GUILD_REQUEST` (v72 125) — v61 opcode 125 is the marriage-decline family
  (`sub_837578` etc); the guild decline is folded into `GUILD_OPERATION=114`
  (`OnGuildResult` reply), no separate opcode.

**2 FLAGGED-UNRESOLVABLE:**
- `SPAWN_PET` (v72 97, `SendActivatePetRequest`, body `[E4,E2,E1]`). Searched: the
  full ctor-xref harvest (no push/mov-imm resolves to a pet-activate), every
  `*Pet*`-named ctor caller (only DoAction/PET_CHAT, PetQ/PET_AUTO_POT — no
  activate), and every v61 opcode with signature `[E4,E2,E1]` (only op90 =
  SPECIAL_MOVE, `CSkillInfo::GetSkillLevel` skill-cast — not pet). Pets are
  otherwise fully present (MOVE_PET/PET_CHAT/PET_LOOT/PET_AUTO_POT/PET_COMMAND/
  PET_EXCLUDE all resolved). The activate sender is either inlined with a
  runtime-computed opcode or absent from the harvest reach; needs a live capture
  or wider-window disasm to confirm — not fabricated.
- `SKILL_MACRO` (v72 109) — displaced when op109 was corrected to SPOUSE_CHAT; the
  v61 skill-macro-save send (`[Str,E1]×N`) was not located (only
  `CFuncKeyMappedMan::SaveFuncKeyMap`=CHANGE_KEYMAP=123 exists among
  FuncKey/Macro-named senders). Likely client-only or inlined in v61.

## Run record

- IDB: GMS_v61.1_U_DEVM.exe @ port 13338 (active). CSV v61 column = placeholder.
- Clientbound `--apply` harvested 140 numeric-switch ops (CNpcPool held out for the
  0x4E gate); 80 manual entries added by hand-decompile. Total 220.
- Serverbound COutPacket-ctor harvest (@0x5ffc4f) → 353 caller functions; 69 mapped
  by FName-join (Stage B). Stage B2 resolved the 43 remaining by **body-signature
  matching against v72's verified opcodes**: 38 resolved, 3 pre-existing mislabels
  corrected (+2 displaced-op additions), 4 dropped-absent, 2 flagged (SPAWN_PET,
  SKILL_MACRO).
- Registry `gms_v61.yaml`: **326 entries** (220 clientbound + 106 serverbound).
  YAML self-validates; no duplicate opcodes within either direction (verified by
  parse + per-direction opcode Counter).
