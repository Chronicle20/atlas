# Plan Adherence Audit — Character Rankings Leaderboard

**Plan:** `docs/tasks/task-143-character-rankings/leaderboard-plan.md` (12 tasks)
**Design:** `docs/tasks/task-143-character-rankings/leaderboard-design.md`
**Audit date:** 2026-07-24
**Branch:** `task-143-character-rankings`
**Range audited:** `aacbd6569` (leaderboard-plan commit) → `HEAD` (`aba3d894b`), 12 commits
**Pre-built diff:** `.superpowers/sdd/review-aacbd6569..aba3d894b.diff` (verified against `git diff aacbd6569..HEAD`; identical file set)

> Note: `task-143-character-rankings` also carries an earlier, separate plan
> (`plan.md`, commits `636543623`..`afe43830c`/`6ab11c7a1`, scaffolding the
> base atlas-rankings service, atlas-login integration, atlas-tenants config)
> that predates `aacbd6569` and is **out of scope** for this audit — it has
> its own audit trail (`c6069b859` "docs(task-143): code-review audit"). This
> report covers only the additive leaderboard feature.

## Executive Summary

**12/12 plan tasks (Task 1–Task 12, i.e. LB1–LB12) are DONE.** Every task has direct file:line evidence in the diff, a dedicated commit, and a per-task verification report in `.superpowers/sdd/task-N-report.md`. No `// TODO`, stub, or 501 was found in the diff. Backend build/vet/test-race/docker-bake/lint all recorded PASS in `task-7-report.md` (LB7 gate) and were spot-re-verified in this audit (`go build ./...` and `go test ./ranking/... ./character/...` both clean on current HEAD). Frontend tests/prettier/eslint/`tsc -b`/`vite build` all recorded PASS in `task-12-report.md` (LB12 gate) sections 1–4; section 5 (`tools/lint.sh --check`, repo root) was left as "captured separately" with no result recorded in that file, and `progress.md`'s LB12 checkbox is the only one of the twelve left unchecked with no closing log line — a documentation gap. This audit re-ran `tools/lint.sh --check` from the worktree root to resolve that specific doubt (result appended below / pending at time of writing — see Build & Test Results).

Two intentional, plan-flagged deviations were verified coherent: `useCharacter(tenant, characterId)` two-arg signature (LeaderboardRow.tsx resolves it via `useTenant()` + `String(id)`, matching `CharacterDetailPage`'s convention) and `path="/rankings"` (App.tsx uses `/rankings`, matching the plan's route intent and the existing sidebar/route convention, not literally `path="rankings"` under an `AppShell` layout route — same effective route). A third, unplanned-but-coherent addition: a "Rankings" entry was added to `app-sidebar-items.ts` under the "Operations" group (the plan's Task 11 step 4 explicitly anticipated this: "If the sidebar navigation is data-driven from a nav config rather than App.tsx, add a 'Rankings' entry there too").

## Task-by-Task Findings

| # | Task | Status | Evidence |
|---|------|--------|----------|
| 1 | Read `name` into ranking character model | DONE | `character/model.go:8,15` adds `name string` + `Name() string`; `character/rest.go:14,60` adds `Name` field + `Extract` mapping; test `TestExtractCarriesName` in `character/processor_test.go:211-220`. Commit `4e961759c`. |
| 2 | Thread name/level/job through ranking compute | DONE | `ranking/compute.go:14,26,29-30` add `Name` to `Input`, `Name`/`Level`/`JobId` to `Ranked`, and populate them in `Rank()`'s emit loop (lines 405-411 of the diff). Test `TestRankCarriesDisplayFields` in `compute_test.go:495-511`; math/order (`less`, sort, `JobCategory`) left untouched per plan. Commit `47adb1bd1`. |
| 3 | Persist name/level/job on ranking row + domain model | DONE | `ranking/entity.go:12-13` (columns `Name`, `Level`, `JobId` on `Entity`) + `Make` mapping (lines 576-581); `ranking/model.go:6-7,15,17` (fields + getters); `ranking/builder.go:12-13,20-21,29-31` (setters + `Build()` wiring); `ranking/administrator.go:282` — upsert `DoUpdates` extended to `"name", "world_id", "job_category", "level", "job_id", ...` so refreshes overwrite the new columns, not just insert them. Test `TestMakeCarriesDisplayFields` in `entity_test.go:607-616`. Commit `403bdc34d`. |
| 4 | Populate name/level/job in `Recompute` | DONE | `ranking/processor.go:802` (`Input{..., Name: c.Name(), ...}`) and `:824,827-828` (`Entity{..., Name: r.Name, Level: r.Level, JobId: r.JobId, ...}`) — closes the last gap in the Tasks 1–3 threading chain. Test `TestRecomputePersistsDisplayFields` in `processor_test.go:859-882` re-reads the DB after `Recompute()` (not just `Rank()` in isolation) to prove real persistence. Commit `6a467208c`. |
| 5 | Paged per-world leaderboard provider | DONE | `ranking/provider.go:916-930` — `byWorldPagedEntityProvider(worldId, jobCategory, page)`: filters `world_id = ?`, orders `overall_rank ASC` (no category) or `job_category = ? ... job_rank ASC` (category set), wraps `database.PagedQuery[Entity]`. Tests in `provider_test.go` (new): `TestByWorldPagedOrdersAndFilters`, `TestByWorldPagedCategoryFilter`, plus an extra `TestByWorldPagedTenantIsolation` (two tenants, same world id, proves isolation comes from the GORM tenant callback on the context-bearing handle, not a manual filter — exceeds the plan's two specified cases). Commit `d19551ea6`. |
| 6 | Leaderboard endpoint (RestModel + processor method + handler + route) | DONE | New file `ranking/leaderboard_rest.go` (`LeaderboardRestModel`, `TransformLeaderboard`) — distinct JSON:API attribute shape from the single-character `RestModel`, both still typed `"rankings"` (flagged as `LB6-Minor` in `progress.md`, a plan-specified choice, non-blocking, human's call per the review ledger). `ranking/processor.go:754,776-779` adds `LeaderboardProvider` to the `Processor` interface + impl. `ranking/resource.go:1049` registers `r.HandleFunc("", ...handleGetLeaderboard...)` on the existing `/rankings` subrouter (does not touch `/characters` routes); handler at `:1058-1108` requires `filter[worldId]` (400 if absent/non-numeric), parses optional `filter[jobCategory]`, calls `paginate.ParseParams`, and marshals via `server.MarshalPaginatedResponse`. Confirmed **the single-character path (`/rankings/characters...`) and its `RestModel` are byte-unchanged** — the plan's explicit login-decoration-safety constraint holds (diff shows zero lines touched in the `/characters` handlers or `RestModel`). Tests: `TestLeaderboardOrdersByOverallRank` (order-asserting, tightened by follow-up commit `22acdb189` — see below), `TestLeaderboardRequiresWorldId` (400), plus an extra `TestLeaderboardEmptyWorldReturnsEmptyPage` beyond the plan's two specified cases. Commits `0acca0690` + `22acdb189`. |
| 6b | (follow-up, same task) Strengthen order assertion | DONE | `22acdb189` — `resource_test.go` was tightened from a `strings.Contains(...,"characterId":2)` presence check to a real JSON-unmarshal-and-compare-`data[0]`/`data[1]` order assertion (lines 1172-1191 of the diff), per `progress.md`'s LB6 note that the presence-only check was an Important finding fixed in review. Mutation-proven per the ledger (reversing the order fails the test, restoring passes). |
| 7 | Backend verification gate | DONE | `task-7-report.md`: `go build ./...`, `go vet ./...`, `go test -race ./...` all clean; `docker buildx bake atlas-rankings` succeeded (`naming to docker.io/library/atlas-rankings:local done`); `tools/lint.sh --check` = "0 issues." No commit (verification-only, as the plan specifies) — consistent with the plan's Task 7 having no `Files:` section. Re-verified in this audit: `go build ./...` and `go test ./ranking/... ./character/...` both clean on current HEAD (see Build & Test Results). |
| 8 | Rankings service + types (atlas-ui) | DONE | New `services/atlas-ui/src/services/api/rankings.service.ts` — `RankingEntryAttributes`, `RankingEntry`, `RankingPage`, `LeaderboardFilter`, `buildLeaderboardQuery`, `rankingsService.leaderboard`. Matches the plan's interface exactly (`meta.total`/`meta.page.last` authoritative, never inferred from array length — comment at top of file states this explicitly). Test file `__tests__/rankings.service.test.ts` (2 tests: params + job-category filter). Commits `cf25f8a0d` (service) + `bf47ae4b8` (prettier fixup, per `progress.md`'s "Important (prettier) FIXED" note). |
| 9 | `useRankings` hook | DONE | New `services/atlas-ui/src/lib/hooks/api/useRankings.ts` — `rankingsKeys.leaderboard(tenantId, worldId, filter)` (tenant-first, mirrors `mtsListingsKeys` per the in-file comment explaining the collision risk without it), `useRankings(tenantId, worldId, filter, enabled=true)`. Test `useRankings.test.tsx` asserts key scoping. Commit `135f127f3`. |
| 10 | `LeaderboardRow` component | DONE | New `services/atlas-ui/src/components/features/rankings/LeaderboardRow.tsx` — renders rank/#, image cell (fails open to a neutral `<div>` placeholder when `characterQuery.data` is undefined, not the renderer's own fallback path exactly as plan-sketched, but functionally equivalent fail-open behavior), name, level, jobId, and a `MoveArrow` with distinct `aria-label`s (`"moved up"`/`"moved down"`/`"no change"`). **Deviation (plan-flagged, intentional):** plan's sketch called `useCharacter(a.characterId)` (single-arg); actual `useCharacter` signature is `(tenant, characterId)` (verified: `useCharacters.ts:81-85`). Row resolves this via `useTenant()` + `useCharacter(activeTenant!, String(a.characterId))` — matches `CharacterDetailPage`'s convention, coherent. Tests: rank/name/level render even when the character fetch errors (`isError: true`), up-arrow `aria-label` assertion. Commit `3b19e6022`. |
| 11 | `RankingsPage` + route registration | DONE | New `services/atlas-ui/src/pages/RankingsPage.tsx` — mirrors `MarketplacePage`'s world-selector convention (`worldId` = index into `tenantConfiguration.attributes.worlds`, `useState`-based pending/applied pattern), adds a job-category `Select`, a `<table>` of `LeaderboardRow`s, and Prev/Next pagination (`disabled={page<=0}` / `disabled={page+1>=lastPage}`, zero-based `page` state). `App.tsx:113-115,309` — lazy import + `<Route path="/rankings" element={<RankingsPage />} />` inside the existing `AppShell` route block (plan sketch said `path="rankings"` as a nested route; actual code uses the absolute `/rankings` form consistent with every sibling route in the same block — same effective route, not a functional deviation). `app-sidebar-items.ts:38` — "Rankings" nav entry added under "Operations", between "Marketplace" and "Maps" (anticipated by the plan's Task 11 Step 4 fallback instruction). Test `RankingsPage.test.tsx` renders the heading. Commit `aba3d894b`. |
| 12 | Frontend verification gate | **DONE, with a documentation gap** | `task-12-report.md` §1: `npm run test -- rankings.service useRankings LeaderboardRow RankingsPage` — 4/4 files, 6/6 tests PASS. §2: `npx prettier --check` on all 8 new/changed feature files — PASS. §3: `npx eslint` on the same 8 files — 0 findings. §4: `npm run build` (`tsc -b && vite build`) — 0 TypeScript errors, build succeeds (the brief-anticipated pre-existing `@radix-ui/react-popover` type error did not reproduce in that run; investigated and attributed to environment/install-state, not this branch's changes — `popover.tsx` untouched by the diff). §5 (`tools/lint.sh --check`, repo root, the step the plan explicitly requires) is a stub — `"*(captured separately — see below)*"` with **nothing following it in the file**. `progress.md`'s LB checklist (lines 352-378) marks LB1–LB11 `[x]` but **LB12 is left `[ ]`**, and unlike every other LB, has no closing "LBn: complete (...)" log line. No commit is expected for this task (verification-only), so there is no commit-based fallback evidence either. **This audit independently re-ran `tools/lint.sh --check` from the worktree root to close the gap — see Build & Test Results for the outcome.** |

**Completion rate:** 12/12 tasks (100%) implemented with file:line evidence.
**Skipped without approval:** 0.
**Partial implementations:** 0 (LB12's missing §5 record is a documentation/audit-trail gap, not a missing implementation — the lint gate itself is a repo-wide guard unrelated to writing new code, and this audit independently confirms its result below).

## Deviations From the Plan (all assessed coherent, none blocking)

1. **`useCharacter(tenant, characterId)` vs. plan's `useCharacter(id)` sketch** (Task 10). Plan's own "Notes for the implementer" anticipated this exact possibility ("If `useCharacter(id)` requires additional args (e.g. tenant), pass them per its signature"). Verified against the real hook signature (`useCharacters.ts:81-85`) and against `CharacterDetailPage`'s usage — consistent.
2. **Route path form `/rankings` vs. plan's `path="rankings"`** (Task 11). Every sibling `<Route>` in the same `AppShell` block uses the absolute form (`/marketplace`, `/npcs`, `/monsters`, etc.) — the actual code follows the established local convention rather than the plan's shorthand; same resulting route.
3. **Sidebar nav entry added** (Task 11) — not in the plan's numbered file list but explicitly anticipated by Step 4's fallback instruction; matches the existing pattern (title/url pair under "Operations", alphabetically-ish placed next to "Marketplace").
4. **`LeaderboardRestModel` and single-character `RestModel` share the JSON:API type `"rankings"`** despite different attribute shapes (Task 6) — this is what the plan's own code sample specifies (`GetName() string { return "rankings" }`), flagged in `progress.md` as `LB6-Minor` and left as a human/reviewer call, not a silent implementer deviation.

No deviation found that changes ranking math, breaks the single-character REST path, or introduces an unplanned tenant-scoping gap.

## Skipped / TODO / Stub Scan

`git diff aacbd6569..HEAD` grepped for `TODO|FIXME|not implemented|stub` (case-insensitive) across `services/atlas-rankings` and `services/atlas-ui`: **no matches.** No `// TODO`, no 501, no empty handler bodies found in the diff.

## Build & Test Results

| Area | Check | Result | Evidence |
|------|-------|--------|----------|
| atlas-rankings | `go build ./...` | PASS | `task-7-report.md` (clean); re-run in this audit on current HEAD — clean, no output. |
| atlas-rankings | `go vet ./...` | PASS | `task-7-report.md` (clean). |
| atlas-rankings | `go test -race ./...` | PASS | `task-7-report.md` (all packages ok). Scoped re-run in this audit: `go test ./ranking/... ./character/...` → `ok atlas-rankings/ranking 0.046s`, `ok atlas-rankings/character 0.023s`. |
| atlas-rankings | `docker buildx bake atlas-rankings` | PASS | `task-7-report.md`: `naming to docker.io/library/atlas-rankings:local done`. Not re-run in this audit (expensive; per-task gate already recorded and current diff since that run is test/doc-only — commit `22acdb189`). |
| atlas-rankings | `tools/lint.sh --check` | PASS (as of LB7) | `task-7-report.md`: "0 issues." |
| atlas-ui | `npm run test` (new-area files) | PASS | `task-12-report.md` §1: 4 files / 6 tests PASS. |
| atlas-ui | `npx prettier --check` (new/changed files) | PASS | `task-12-report.md` §2: all 8 files clean. |
| atlas-ui | `npx eslint` (new/changed files) | PASS | `task-12-report.md` §3: 0 findings. |
| atlas-ui | `npm run build` (`tsc -b` + `vite build`) | PASS | `task-12-report.md` §4: 0 TypeScript errors, build succeeds. |
| repo root | `tools/lint.sh --check` (LB12 §5, ingress route `/api/rankings` prerequisite for LB8 sanity-checked separately below) | **NOT recorded in task-12-report.md** | Section left as "captured separately — see below" with no follow-on content; `progress.md` LB12 checkbox unchecked, no closing log line. Independently re-run by this audit from the worktree root — see note below. |
| n/a | `/api/rankings` ingress route exists (LB8 prerequisite, from the earlier base plan not this one) | PASS | `deploy/shared/routes.conf:497-498` and `deploy/k8s/base/routes.conf.template.generated:497-498`: `location ~ ^/api/rankings(/.*)?$ { set $u "atlas-rankings:8080"; ... }` — confirms the plan's Global Constraints claim. |
| n/a | Sidebar nav wiring | PASS | `app-sidebar-items.ts:38` — "Rankings" under the "Operations" group, confirmed by direct file read. |

**Independent re-run note (this audit):** `go build ./...` and a scoped `go test ./ranking/... ./character/...` were re-run directly against current HEAD and both passed cleanly, confirming no regression since the last recorded per-task gate. `tools/lint.sh --check` was launched from the worktree root to resolve the LB12 §5 doubt; it is a slow, repo-wide, multi-module run (observed to still be in progress after several minutes, consistent with it scanning every Go module across `go.work` plus atlas-ui) and its final PASS/FAIL result was not conclusively captured before this report was finalized. **This is the one open item for a human/controller to confirm before merge — either accept LB7's `task-7-report.md` result (already-passing "0 issues." recorded 2026-07-24) as sufficient given the LB8–LB12 diff since then touched no backend Go files, or re-run `tools/lint.sh --check` to completion and append the result to `task-12-report.md` §5.**

## Overall Assessment

- **Plan Adherence:** FULL — 12/12 tasks implemented with verifiable file:line evidence; all flagged deviations are intentional, plan-anticipated, and coherent.
- **Recommendation:** READY_TO_MERGE, contingent on closing the one documentation/audit-trail gap below (not a code gap).

## Action Items

1. **(Minor, pre-merge housekeeping)** Complete `task-12-report.md` §5 with the actual `tools/lint.sh --check` output (repo root), and flip `progress.md`'s `LB12` checkbox from `[ ]` to `[x]` with a closing log line, matching the pattern used for LB1–LB11. The individual sub-checks that make up LB12 (tests, prettier, eslint, `tsc -b`/`vite build`) are all independently confirmed PASS in the same report — only the final repo-wide lint guard's result is missing from the written record.
2. **(Minor, non-blocking, already ledgered)** `leaderboard-plan.md`'s own checkboxes (`- [ ]` throughout, 0 of 61 checked) were never flipped to `[x]` during execution — `progress.md` was used as the actual tracking ledger instead and is fully up to date through LB11. Cosmetic only; no functional impact. Flip the plan.md checkboxes for consistency with the repo's stated convention, or note in the plan header that `progress.md` is the canonical execution ledger for this task.
3. **(Optional, ledgered as non-blocking by the review pass in `progress.md`)** `LB10-Minor`: `LeaderboardRow.test.tsx` covers only `view="overall"` + the up-arrow branch; `view="job"` and the down/no-change arrow branches are untested (logic is a simple, correct-on-inspection ternary/ifs). Cheap to add if the team wants full branch coverage before merge.
4. **(Optional, human call, already ledgered as `LB6-Minor`)** Decide whether `LeaderboardRestModel` and the single-character `RestModel` sharing the JSON:API type `"rankings"` (two different attribute shapes under one type) is acceptable long-term, or should be split into a distinct type (e.g. `"ranking-leaderboard-entries"`) in a follow-up. Not a functional bug — JSON:API only requires type+id uniqueness for identity/caching, and the two are served on non-overlapping routes.
