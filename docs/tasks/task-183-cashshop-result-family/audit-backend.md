# Backend Audit — task-183 cashshop-result-family (Go changes)

- **Scope:** `libs/atlas-packet/cash/clientbound/shop_operation_result_{failed,slots,gift,misc,transfer,gachapon}.go` (+ `_test.go`), `shop_operation_body.go`, `shop_inventory.go` (0x4D stub removal), `tools/packet-audit/cmd/run.go`, `services/atlas-channel/.../cash_shop_entry.go` (dead-call cleanup)
- **Worktree:** `.worktrees/task-183-cashshop-result-family`
- **Base:** `cdfb71aa317cbec966709a85f5811cb886a18bb5`
- **Guidelines Source:** backend-dev-guidelines skill, focus areas as specified by dispatcher (DOM-25, discrete-per-mode, immutability, version-gating idiom, DOM-21, test hygiene)
- **Date:** 2026-07-27
- **Build:** PASS (`go build ./...` in `libs/atlas-packet` and `tools/packet-audit`, exit 0)
- **Tests:** PASS (`go test ./cash/... -count=1` in `libs/atlas-packet` — `cash/clientbound` and `cash/serverbound` both `ok`)
- **Overall:** PASS

Note on scope: this is a `libs/atlas-packet` codec library, not a service with `model.go`/`processor.go`/`resource.go`. The generic DOM-01..DOM-24 service checklist (Processor/RestModel/handler-layer checks) does not structurally apply — there is no REST/Kafka/DB layer in this diff. Findings below are scoped to the dispatcher's explicit focus areas, which map to DOM-25 (client wire values config-resolved) and the surrounding packet-codec conventions (discrete-per-mode, immutability, version-gating idiom, DOM-21 type reuse, test hygiene).

## Findings

| ID | Check | Status | Evidence |
|----|-------|--------|----------|
| DOM-25a | Every body func in `shop_operation_body.go` resolves its mode byte via `WithResolvedCode("operations", KEY, ...)` or `ResolveCode(l, options, "operations", KEY)` — never a literal | PASS | `libs/atlas-packet/cash/clientbound/shop_operation_body.go` — 82 call sites of `ResolveCode`/`WithResolvedCode` across ~41 body funcs (one mode + one reason-code call per failure-arm body, one mode call per success/scalar-arm body). `grep -n "WriteByte(0x\|WriteByte([0-9]"` over all six changed struct files + `shop_operation_body.go` returns zero matches — no hardcoded mode/opcode literal anywhere in `Encode`. |
| DOM-25b | Failure-arm reason byte resolved via `ResolveCode(..., "errors", message)`, never a caller-supplied numeric code | PASS | e.g. `shop_operation_body.go:247-249` (`CashShopBuyFailedBody`) — `errorCode := atlas_packet.ResolveCode(l, options, "errors", message)`; identical pattern repeated for all 24 failure arms (`shop_operation_body.go:211-445`). |
| DOM-25c | No `func(_ byte)` / no body func accepting a caller-supplied op/mode/key | PASS | `grep -n "func(_ byte)"` over `libs/atlas-packet/cash/clientbound/*.go` returns zero matches. Every `NewXxx(mode byte, ...)` constructor takes `mode` only to be threaded through by the already-resolved body func — no body func signature accepts a raw mode/operation-key parameter from its caller. |
| DISCRETE-1 | One struct per dispatcher arm, discrete even where wire shapes coincide (bodyless arms are `struct{mode byte}`) | PASS | 25 discrete failure structs in `shop_operation_result_failed.go` (`grep "^type "` → `LoadGiftFailed` … `ChangeMaplePointFailed`, `shop_operation_result_failed.go:17-876`), each independently defined even though most share the identical mode+errorCode wire shape (comment at `shop_operation_body.go:206-209` states the INV-1 discrete-struct invariant explicitly). Bodyless `ChangeMaplePointFailed` is `struct{ mode byte }` (`shop_operation_result_failed.go:872-876`). Same pattern holds in `_slots.go`, `_gift.go`, `_misc.go`, `_transfer.go`, `_gachapon.go` — confirmed by reading all six files in full. |
| IMMUT-1 | Structs are immutable: private fields, public getters, constructor, no setters | PASS | `grep -n "^func (m \*.*) Set"` over all six new struct files returns zero matches. All fields are unexported (`mode byte`, `recipientName string`, etc. — e.g. `shop_operation_result_gift.go:107-112`); every struct exposes only a `NewXxx(...)` constructor plus value-receiver getters (e.g. `GiftDone.Mode()`, `.RecipientName()` at `shop_operation_result_gift.go:118-122`). The only field mutation is inside each struct's own pointer-receiver `Decode` method, which is the standard read-path for this packet-codec library (not an external setter). |
| GATE-1 | jms nxCashSpent gate (GiftDone/GiftPackageDone) uses region idiom, not raw version compare | PASS | `giftHasNxCashSpent(t tenant.Model) bool { return t.Region() == "GMS" }` at `shop_operation_result_gift.go:92-94`, applied in both `GiftDone.Encode/Decode` (`shop_operation_result_gift.go:137,151`) and `GiftPackageDone.Encode/Decode` (`shop_operation_result_gift.go:408,423`). Region-only gate, no numeric literal compare. |
| GATE-2 | v72 IncBuyCharacterCountSuccess slot+asset gate uses region+exact-version idiom, not raw `>N` | PASS | `incBuyCharacterCountSuccessIsV72Shape(t tenant.Model) bool { return t.Region() == "GMS" && t.MajorVersion() == 72 }` at `shop_operation_result_slots.go:154-156`, with an explicit doc comment (`:150-153`) justifying exact-match over `MajorAtLeast` ("a one-version divergence, not a floor"). Applied in `Encode`/`Decode` at `shop_operation_result_slots.go:163,177`. |
| GATE-3 | No raw `MajorVersion() > N` literal comparisons introduced in the diff | PASS | `grep -n "MajorVersion() >"` across `libs/atlas-packet/cash/clientbound/*.go` finds hits only in pre-existing, unchanged code (`shop_open.go`, `query_result.go`, and the `>=95` gate at `shop_inventory.go:134`, all outside this diff's line ranges — `shop_inventory.go`'s only diff hunk is the deletion of `CashShopGifts` at the end of the file, see `git diff` below). None of the 6 new/changed struct or body files add a raw `>` version compare. |
| DOM-21a | 55-byte item-blob arms reuse `CashInventoryItem`, not a new type | PASS | `type CashInventoryItem struct` defined once, pre-existing, at `shop_inventory.go:16`; every new item-blob arm (`FriendshipDone`, `CoupleDone`, `FreeCashItemDone`, `NameChangeBuyDone`, `TransferWorldDone`, `GachaponOpenDone.NewItem`, `GachaponCopyDone.Item`, gift-family list arms) declares its field as `CashInventoryItem` and decodes via the pre-existing `decodeCashInventoryItemSkipPadding` (`shop_inventory.go:55`) — e.g. `shop_operation_result_gift.go:484`, `shop_operation_result_misc.go:191`, `shop_operation_result_transfer.go:30,70`, `shop_operation_result_gachapon.go:38,108`. No parallel/duplicate item-blob type introduced. |
| DOM-21b | v72 asset arm reuses `packetmodel.Asset`, not a new type | PASS | `shop_operation_result_slots.go:9` imports `packetmodel "github.com/Chronicle20/atlas/libs/atlas-packet/model"`; `IncBuyCharacterCountSuccess.asset` is typed `packetmodel.Asset` (`shop_operation_result_slots.go:125`), the pre-existing shared asset codec (`libs/atlas-packet/model/asset.go:17`) — not reimplemented locally. |
| DOM-21c | New helper types `GiftListEntry`/`PackedCashItemRef` are genuinely new wire shapes, not atlas-constants duplicates | PASS | `grep -rln "PackedCashItemRef\|GiftListEntry" libs/atlas-constants/` returns no hits — these are packet-wire-layout records (98-byte `GW_GiftList`, 8-byte packed ref) with no equivalent in `libs/atlas-constants`, and both carry doc comments citing the exact decompile/type_inspect evidence for their byte layout (`shop_operation_result_gift.go:24-37,57-68`). |
| TEST-1 | Byte-fixture tests assert real encoded bytes, not mocked | PASS | Every `*ByteFixture` test constructs `want := []byte{...}` from the actual field values and compares against `pt.Encode(...)` output, e.g. `shop_operation_result_failed_test.go:184` (`want := []byte{mode, errorCode}`), `shop_operation_result_slots_test.go:94` (`want := []byte{mode, byte(trunkCount), byte(trunkCount >> 8)}`), plus a `pt.RoundTrip` decode-and-compare in the same test. |
| TEST-2 | No `*_testhelpers.go` files | PASS | `find libs/atlas-packet/cash/clientbound -iname "*testhelper*"` returns nothing. |
| TEST-3 | Table-driven style (`pt.Variants` + `t.Run`) | PASS | All six changed test files iterate `for _, v := range pt.Variants { t.Run(v.Name, func(t *testing.T) {...}) }` — e.g. `shop_operation_result_failed_test.go:175-176`, confirmed present (`t.Run(` count ≥3 per file) in every changed test file per the grep sweep. |
| BUILD-1 | `go build ./...` clean | PASS | `libs/atlas-packet`: exit 0. `tools/packet-audit`: exit 0. |
| TEST-4 | `go test ./... -count=1` clean | PASS | `libs/atlas-packet/cash/clientbound`: ok. `libs/atlas-packet/cash/serverbound`: ok. |
| FMT-1 | `gofmt -l` clean on changed files | PASS | `gofmt -l libs/atlas-packet/cash/clientbound/*.go` — no output. |
| VET-1 | `go vet ./...` clean | PASS | `libs/atlas-packet`: no output. |
| GOROUTINE-1 | No bare `go` statements introduced | PASS | `grep -rnE '^\s*go (func|[A-Za-z_])'` over the changed non-test files in scope returns zero matches. |
| DEADCODE-1 | `cash_shop_entry.go` handler change is dead-code cleanup only, not a behavior removal | PASS | `git diff` shows only a commented-out `session.Announce(...CashShopCashGiftsBody...)` block deleted (`services/atlas-channel/.../cash_shop_entry.go:99-103` pre-change) — the call was already dead (commented out) before this branch; no live gift-announce path removed. |

### Minor / non-blocking observation (not a guideline violation)

- `packet-audit:verify` marker placement is inconsistent across the six changed test files: most files place the marker block immediately above each `func Test...ByteFixture` (e.g. `shop_operation_result_failed_test.go:163-171` directly precedes `TestBuyFailedByteFixture` at `:173`), but `shop_operation_result_slots_test.go` instead batches all four arms' markers into one block at the top of the file (`shop_operation_result_slots_test.go:13-38`) before any of the four covered test functions (`:83,107,131,190`). No documented convention mandates per-function marker proximity, and the markers are present and complete for every arm in the file, so this is **not scored as a finding** — flagged only for consistency awareness, not required to fix.

## Summary

### Blocking (must fix)
- None.

### Non-Blocking (should fix)
- None (see marker-placement observation above, explicitly non-blocking).
