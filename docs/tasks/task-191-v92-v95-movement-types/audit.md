# Plan Audit — task-191-v92-v95-movement-types

**Plan Path:** docs/tasks/task-191-v92-v95-movement-types/plan.md
**Audit Date:** 2026-08-05
**Branch:** task-191-v92-v95-movement-types
**Base / merge-base:** 999e48a2a (local `main` has advanced past the fork point; all scope comparisons below use the merge-base per the audit brief, not `main`)
**Commits on branch:** 16 (`git log --oneline 999e48a2a..HEAD`)

## Executive Summary

All 10 plan tasks (Task 1 derivation through Task 10 live reconcile) are implemented and evidenced in the diff, matching the plan's intent — including the five approved deviations called out in the audit brief (five movement handlers not four, the `version_bounds_test.go` strengthening, the five v95 validator fixes, the v48 live-tenant extension, and Task 8/Task 9's controller-verified scope). `go build`/`go vet`/`go test -race ./... -count=1` are clean in `libs/atlas-packet` (the only Go module touched, no `go.mod` change, so no `docker buildx bake` round is required), both template guards are green (`template-opcode-order-guard.sh` reports 22 ascending arrays, `template-movement-types-guard.sh` reports the expected **54** move-handler entries across 11 templates), and `tools/lint.sh --check` is established clean (exit 0, zero files touched) per Task 9's report — my own independent lint invocation collided with a concurrent `golangci-lint` process from an unrelated worktree session and was abandoned rather than re-derived, per instruction. No genuine gaps were found against the plan; the only process nit is that `plan.md`'s 80 checkboxes were never ticked despite the work being done (tracking hygiene, not a functional gap). All five previously-flagged deferred/pre-existing items are safe to leave as documented and are not merge-blocking.

## Task Completion

| # | Task | Status | Evidence |
|---|------|--------|----------|
| 1 | Derive movement tables, header delta, opcodes from v92/v95 clients | DONE | `movement-types-derivation.md` — all 11 required sections present: §1 IDB session map (L13-39), §2 method + v83 control PASS 23/23 (L43-98), §3 v95 element table incl. per-index table indices 0-36 (L102-181), §4 v92 element table derived independently, confirmed identical to v95 index-for-index (L184-259), §5 header delta incl. negative JMS read (L263-286), §6 opcodes incl. 571/597-site sweep + 14-site cross-validation + full v92 summon-block derivation + `0xC8/0xC9/0xCA` ownership resolution (L290-531), §7 IDB renames — 11 applied, `dir:"vibe"` (L534-555), §8 unresolved indices "None" (L558-567), §9 divergences from Appendix A "None" + §9.1 prose correction (NPCActionHandle is the 5th handler) (L571-607), §10 matrix note re: stale fixture markers (L611-632), §11 client-option coupling (L636-673) |
| 2 | Add v88+ `StartVx`/`StartVy` movement header | DONE | `libs/atlas-packet/model/movement.go:27-34` (struct fields), `:36-64` (`Decode` gate at `:61-64`, `t := tenant.MustFromContext(ctx)` at `:37` outside the closure), `:214-227` (`Encode` gate at `:224-227`, tenant resolved at `:216`). Gate text `t.IsRegion("GMS") && t.MajorAtLeast(88)` is byte-for-byte identical in both directions. JMS correctly excluded (opposite shape from the `XOffset`/`YOffset` gate at `:160`/`:260`, left untouched). Tests: `movement_test.go:83-112` (`TestMovementHeaderVersionBoundary`) and `:116-143` (`TestMovementHeaderRoundTrip`) pin both sides with literal bytes including the JMS-drops/v92-v95-carries distinction; both PASS (verified independently, see Verification below). |
| 3 | Add movement-types guard, demonstrated RED then wired | DONE | `tools/template-movement-types-guard.sh:37-43` — `MOVE_HANDLERS` set has all five (`CharacterMoveHandle`, `MonsterMovementHandle`, `NPCActionHandle`, `PetMovementHandle`, `SummonMoveHandle`), matching the derivation's §9.1/§6.5 finding. Commit `9f2152b59` demonstrates RED; commit `226ae84aa` adds a per-template contribution floor (a strengthening found during Task 3's own review round, not a plan requirement — harmless, documented in its commit message). Guard now exits 0 reporting **54** move handlers across 11 templates (reproduced independently below), matching the audit brief's approved deviation-1 total exactly. |
| 4 | Close v48 `SummonMoveHandle` leftover | DONE | `template_gms_48_1.json:733` `SummonMoveHandle` `0x78`, `validator: LoggedInValidator`, `options.types` length 23 (verified via `python3` probe). All five v48 move handlers (`CharacterMoveHandle`, `PetMovementHandle`, `SummonMoveHandle`, `MonsterMovementHandle`, `NPCActionHandle`) report `identical=True`, length 23. Diff confined to `template_gms_48_1.json` only. |
| 5 | Fix `template_gms_92_1.json` | DONE | `CharacterMoveHandle` `0x2E` (`:170-172`) types=37; new `PetMovementHandle` entry `0xC4` (`:432-434`) types=37; `SummonMoveHandle` `0xC8`→`0xCC` (`:590`) types=37; `SummonAttackHandle` `0xC9`→`0xCD` (`:748`, no `types` — correctly not a movement handler); `SummonDamageHandle` `0xCA`→`0xCE` (`:754`, no `types`); `MonsterMovementHandle` `0xDC` (`:760`) types=37; `NPCActionHandle` `0xEA` (`:918`) types=37 (added per the derivation's routing correction). All five move-handler arrays `identical=True`, ascending opcode order confirmed, no trailing newline preserved, no CRLF. |
| 6 | Fix `template_gms_95_1.json` | DONE | New `CharacterMoveHandle` entry `0x2C` (`:177`) types=37; `PetMovementHandle` `0xC7` (`:788`) types=37; `SummonMoveHandle` `0xCF` (`:976`) types=37; `MonsterMovementHandle` `0xE3` (`:1146`) types=37; `NPCActionHandle` `0xF1` (`:1364`) types=37. All five arrays `identical=True` and byte-identical to `template_gms_92_1.json`'s array (per derivation §4.1's "v92 == v95 index-for-index" finding), ascending confirmed. Diff confined to exactly these two templates plus `template_gms_48_1.json` — no other of the 11 templates changed (`git diff --stat 999e48a2a..HEAD -- services/atlas-configurations/seed-data/templates/` shows exactly 3 files). |
| 7 | Wire the guard into CI and docs | DONE | All **four** required CI touchpoints present in `.github/workflows/pr-validation.yml`: (a) job `template-movement-types-guard:` at `:334`; (b) added to `pr-validation-complete`'s `needs:` list at `:734`; (c) rollup variable `TMPL_MOVE_GUARD_RESULT` at `:756` and summary row at `:775`; (d) **the pass/fail assertion** — the silent-failure-mode touchpoint — includes `[ "$TMPL_MOVE_GUARD_RESULT" == "failure" ]` at `:784`. `CLAUDE.md:61-71` adds Build & Verification item 11, correctly naming all five handlers. `docs/packets/TEMPLATE_CONVENTIONS.md:32` adds the "Rule: move handlers carry a `types` table" section (five handlers named, both name-based traps documented) and extends the "Guard" section to reference both scripts. |
| 8 | Correct v95 registry `MOVE_PLAYER` fname | DONE | `docs/packets/registry/gms_v95.yaml` diff: `fname` promoted to `CVecCtrlUser::EndUpdateActive`, `fname_alts` demoted to `[CUserLocal::OnKey]`, `opcode: 44` unchanged — exactly 2 insertions/2 deletions, matching the plan's expected diffstat and Task 1 §6.2's derivation. |
| 9 | Full verification sweep | DONE | `.superpowers/sdd/plan/task-9-report.md` (19KB) documents the complete sweep: `go test -race ./...` all `ok`/no `FAIL` + `go vet ./...` silent in `libs/atlas-packet`; `no go.mod touched`; both template guards green (order 22/22, movement 54/54); `tools/lint.sh --check` exit 0 after sourcing `nvm use 22` (first attempt's `ui:node-missing` was a non-interactive-shell environment gap, not a code issue — Go modules had already reported `0 issues.` on that same pass); FR-5 invariants re-run across all 11 templates; scope containment confirmed against the merge-base; worktree/branch verified clean. I independently reproduced the Go build/vet/test and both guard results below with matching output. |
| 10 | Reconcile live v92/v95 tenants | DONE (extended) | `reconcile.md` has all 9 required elements: §0 full-replace + no-wholesale-swap warnings, §1 environment/namespace/date, §2 tenant ids with region/version (3 tenants across 2 passes), §3 target state, §4 before state, §5 build+diff-proof, §6 PATCH (HTTP 200 all three), §7 quoted read-back output (all five handlers, correct-length `types`, `LoggedInValidator`, ascending), §8 restart + quoted `Configuring opcode`/`listener.added` evidence, §9 quoted negative-signal grep (`0` in both passes, with an honest caveat about zero live traffic), §10 pre-existing defects NOT fixed, §11 fleet-wide sweep + not-reconciled list, §12 repeatability checklist, Appendix A with `build.py`/`build_v48.py`/`diff.py`/`sweep.sh` embedded verbatim. Commit `ce9f70702` (final commit) adds a second pass for GMS 48.1 (FR-4's live counterpart) and **explicitly corrects a false claim** in an earlier revision that non-target tenants were unaffected — replacing a `Configuring opcode`-based inference (proves registration only) with an actual `types`-level fleet sweep. This is the approved deviation-4 extension plus a genuine, well-documented self-correction. |

**Completion Rate:** 10/10 tasks (100%)
**Skipped without approval:** 0
**Partial implementations:** 0

Note: none of `plan.md`'s 80 `- [ ]` checkboxes are ticked (`grep -c '^\- \[x\]' plan.md` → 0), despite every task being demonstrably implemented and committed. This is a plan-tracking hygiene gap, not a functional one — every task's evidence was verified independently against the actual diff, files, and test/guard output rather than trusted from checkbox state. Not merge-blocking, but worth ticking before the plan document is archived.

## Skipped / Deferred Tasks

None. All 10 tasks are DONE.

## Merge-Blocking Triage — previously flagged deferred/pre-existing items

| # | Item | Verdict | Reason |
|---|------|---------|--------|
| 1 | Five movement fixtures (`character/clientbound`, `monster/clientbound`, `monster/serverbound`, `pet/clientbound`, `pet/serverbound` `movement_test.go`) carry `packet-audit:verify version=gms_v95` markers whose comments claim byte-identical structure across v83/v87/v95 — derivation §5 disproves this (v95 header is 4 fields longer, element table is 37 vs 23 entries) | **SAFE TO DEFER** | Explicit PRD §2 non-goal ("no fixture campaign"); correction is recorded in `movement-types-derivation.md` §10 for a future matrix pass. The fixtures assert against `model.Movement.Encode`'s own output (not client-derived bytes) so they pass regardless of the stale comment — a known "round-trip fixture ≠ client-validated" instance, not a live defect this task introduces or worsens. |
| 2 | Guard's `d.get("socket", {}).get("handlers", []) or []` raises `AttributeError` if `socket` is present but a non-dict | **SAFE TO DEFER** | Reproduced directly: `d = {'socket': 'not-a-dict'}` → `AttributeError: 'str' object has no attribute 'get'`. Under the guard's `set -euo pipefail` + unguarded Python heredoc, this crashes the script with a non-zero exit — the failure mode is loud and fails CI, never a vacuous pass. Only reachable via a manually corrupted template. |
| 3 | `reconcile.md` §8 shows captured `Configuring opcode`/`listener.added` log output but not the literal `kubectl logs \| grep` command that produced it | **SAFE TO DEFER** | Documentation completeness nit only. The quoted output itself is present, dated, and tied to specific pod names/restart timestamps; the omitted command is trivially reconstructable (`kubectl -n atlas-main logs deployment/atlas-channel --tail=400 \| grep -iE 'Configuring opcode\|listener.added'`, per plan Task 10 Step 6) and doesn't affect the evidentiary value of what's shown. |
| 4 | Pre-existing (not caused by this branch): `template_gms_95_1.json` registers both `"0x9C" UseDoor"` (line 2286) and `"0x09C" EnterDoorHandle"` (line ~724) — both parse to opcode 156, one silently shadowed; slips past `template-opcode-order-guard.sh` because it compares `code < prev` (non-strict) despite CLAUDE.md item 9 saying "strictly ascending" | **SAFE TO DEFER** | Confirmed present and byte-identical at the merge-base (`git show 999e48a2a:...template_gms_95_1.json \| grep 0x9C`) and untouched by this branch's diff (`git diff 999e48a2a..HEAD -- template_gms_95_1.json` has no hunk near those lines). Deliberately out of scope — fixing needs its own ownership derivation, and tightening the order guard would redden CI on this unrelated pre-existing violation. |
| 5 | Pre-existing: live v95 tenant has 32 handlers with an empty `validator`, dropped at load time | **SAFE TO DEFER** | Recorded in `reconcile.md` §10.1 as "a real functional gap ... deserves its own task." Not caused by this task's PATCH — `writers` and all non-movement `handlers` were verified byte-identical before/after in the diff-proof (reconcile.md §5/§7); the PATCH touched only the five movement-handler entries. |

None of the five items block merge.

## Build & Test Results

| Check | Result | Notes |
|-------|--------|-------|
| `go build ./...` (libs/atlas-packet) | PASS | No output. |
| `go vet ./...` (libs/atlas-packet) | PASS | No output (silent = clean). |
| `go test -race ./... -count=1` (libs/atlas-packet) | PASS | All packages `ok` or `[no test files]`, zero `FAIL`. Movement-specific subset reproduced with `-v`: `TestMovementHeaderVersionBoundary` PASS, `TestMovementHeaderRoundTrip` PASS (all 4 sub-tests: `GMS_v87_drops_vx/vy`, `JMS_v185_drops_vx/vy`, `GMS_v92_carries_vx/vy`, `GMS_v95_carries_vx/vy`), `TestMovementFlyingBlockType23` PASS, `TestMoveVersionBoundary` (`character/serverbound`) PASS. |
| `tools/template-opcode-order-guard.sh` | PASS (exit 0) | `OK: 22 template arrays are in ascending opcode order.` |
| `tools/template-movement-types-guard.sh` | PASS (exit 0) | `OK: 54 move handlers across 11 templates carry a valid movement types table.` — per-template breakdown: `gms_12_1`: 4 (no Pet handler, correct-as-is); all other 10 templates: 5 each. Matches the audit brief's approved deviation-1 expected total exactly. |
| `git diff --name-only 999e48a2a..HEAD \| grep go.mod` | Confirmed absent | `no go.mod touched` — no `docker buildx bake` round required per CLAUDE.md item 4. |
| `git diff --name-only 999e48a2a..HEAD \| grep '^services/atlas-channel/'` | Confirmed absent | `atlas-channel untouched (ok)` — matches the plan's Global Constraint. |
| `tools/lint.sh --check` | PASS (exit 0) | Per `.superpowers/sdd/plan/task-9-report.md`: first attempt failed only on `ui:node-missing` (non-interactive shell doesn't source nvm; every Go module had already reported `0 issues.` on that pass). Re-run with `nvm use 22` sourced: `lint.sh: OK`, exit 0, zero files touched — 6 pre-existing ESLint warnings (0 errors) in atlas-ui files this branch never touched, matching the documented pre-existing atlas-ui lint baseline. My own independent re-invocation collided with a concurrent `golangci-lint` process from an unrelated worktree session (`task-187`/`task-189`/`task-145`/`task-147` all visible in the process list) and errored with "parallel golangci-lint is running" rather than a real code finding; abandoned per instruction rather than re-derived, since Task 9's result is already established and this branch touched no atlas-ui files and no additional Go files beyond `libs/atlas-packet/model/movement*.go` and `character/serverbound/version_bounds_test.go` since that pass. |

Scope containment (`git diff --stat 999e48a2a..HEAD`): 18 files changed — exactly the file set the plan's Task 9 Step 5 and File Structure table predict (workflow YAML, CLAUDE.md, TEMPLATE_CONVENTIONS.md, the v95 registry row, the 7 task-191 docs, `movement.go`/`movement_test.go`, `version_bounds_test.go` (approved scope exception), the 3 templates, and the new guard script). No other service or library touched.

## Overall Assessment

- **Plan Adherence:** FULL
- **Recommendation:** READY_TO_MERGE

## Action Items

None required before merge. Optional, non-blocking cleanup:

1. Tick `plan.md`'s 80 checkboxes (or note in the plan why they were left unticked) for tracking hygiene — purely cosmetic, does not affect the shipped code.
2. Commit `docs/tasks/task-191-v92-v95-movement-types/audit-backend.md` (the backend-guidelines-reviewer's already-finished PASS report) alongside this file as part of Task 11's Step 5 ("commit the audit artifacts").
3. Consider adding the literal `kubectl logs | grep` command line to `reconcile.md` §8 next time it's touched, for exact reproducibility (deferred item 3 above).
