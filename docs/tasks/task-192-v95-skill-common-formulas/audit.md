## Plan Adherence Review

**Plan Path:** docs/tasks/task-192-v95-skill-common-formulas/plan.md
**Audit Date:** 2026-08-07
**Branch:** task-192-v95-skill-common-formulas
**Base Branch:** main (merge-base `e5bdd632e`, branch tip `f82f8305f`, includes a merge of `origin/main`)

### Executive Summary

All 8 plan tasks were faithfully implemented, and every pinning test named in the plan's Requirement Coverage table exists and asserts what it claims (verified by reading the actual test bodies, not just their names). `go build ./...`, `go vet ./...`, and `go test -race ./... -count=1` are all clean in `services/atlas-data/atlas.com/data` (65 packages, 0 failures). `tools/redis-key-guard.sh` and `tools/goroutine-guard.sh` both exit 0 from the repo root. `services/atlas-ui` builds clean (`npm run build`) and the `src/lib/skills` vitest suite passes (23/23). `tools/lint.sh --check` completed: `services/atlas-data` is **not** among its 5 failing targets (grep-confirmed no `lint:services/atlas-data` or golangci-lint failure line references it), so this branch's Go lint is clean; the 5 reported failures are `atlas-merchant`, `atlas-monster-death`, `atlas-summons`, `atlas-tenants` (none touched by this branch — their failures are `Error: parallel golangci-lint is running`, a cross-worktree lock-contention false-fail, matching the known project footgun) and `ui:node-missing`, which is an artifact of that specific background shell not having `nvm use 22` sourced — the dedicated `npm run build` run in this audit (with Node 22 active) succeeded cleanly, so this is not a real atlas-ui lint failure either. Three self-review fixes landed beyond the plan's literal code (all documented, tested, and consistent with project conventions): the `commonKeyDef` bounds table was corrected from `maxUint32` to `maxInt32` per the approved Deviation 5, and the SKILL worker's run-summary log was moved to a `defer` so it fires on walk-level errors too. The two live-cluster steps (Task 8 Steps 3-4: deploy + re-ingest + restart + `GET` assertions) were not performed, per the human's explicit approval (Deviation 4) — reported here as not-performed, not as a gap.

### Task Completion

| # | Task | Status | Evidence / Notes |
|---|------|--------|------------------|
| 1 | `skill/formula` — expression evaluator | DONE | `services/atlas-data/atlas.com/data/skill/formula/formula.go` implements `Parse`, `Expr.Evaluate`, `Expr.Source` exactly per plan §Task 1 Step 3 (346 lines vs. plan's ~330-line listing; package doc comment, precedence tiers `parseAdd→parseSub→parseDiv→parseMul`, `round()`'s ceiling-replaces-truncation semantics all match verbatim). `formula_test.go:8` `TestEvaluate` and `:68` `TestParseRejects`, `:97` `TestEvaluateRejectsNonFinite`, `:107` `TestParseOnceEvaluateMany` all present and match the plan's cases. `Expr.EvaluateFloat` correctly absent (approved Deviation 1). |
| 2 | `effect` model — 35 `common` keys | DONE | `skill/effect/rest.go:69-105` adds `Range` … `MesoR` with JSON tags verbatim to plan (e.g. `ConsumeItemId int32 \`json:"consumeItemId"\`` at rest.go:101, distinct from the pre-existing `ItemConsume`/`itemConsume`). `skill/effect/model.go:69` (`rangeValue` field), `:525-558` (35 setters), `:474+` (`Build()` literal). `skill/effect/model_test.go:11` `TestCommonKeyJSONTags` and `:56` `TestExistingItemConsumeUnchanged` both present, asserting all 35 JSON tags and the `ItemConsume`/`ConsumeItemId` non-collision respectively. |
| 3 | `getEffect` reads the new keys | DONE | `skill/reader.go:268-302`: one `SetXxx(node.GetIntegerWithDefault(...))` chain per key, inserted after the existing `SetMoveTo` chain, exactly as specified. `skill/reader_test.go:3610` `TestLevelPathPopulatesCommonKeys` asserts `Mastery`, `Range`, `Dot`/`DotInterval`/`DotTime`, `MHPRRate`/`MMPRRate`, `ConsumeItemId` vs `ItemConsume` all populate from a `level` fixture — proving the shared-`getEffect` design (FR-6.1) actually holds. |
| 4 | `common` detection, key table, per-level expansion | DONE | `skill/common.go` (333 lines): `commonKeyDef`, `commonKeys` table (65 keys, declared order), `commonKeyIndex`, `synthesizeCommonNodes`, `expandCommon` all present and match the plan's Step 3 listing, modulo the approved Deviation 5 bound fix (see below). `skill/reader.go:129-149` `produceSkill` has the `common`-wins-unconditionally branch ahead of the `level` branch, structural detection only (`ChildByName("common")`, no version/region predicate anywhere in `common.go` or the new `reader.go` block — confirmed by grep). `skill/common_test.go` has all 8 named tests: `TestCommonExpansion` (:39), `TestCommonXKeyIsNotTheVariable` (:84), `TestCommonWinsOverLevel` (:113), `TestCommonMaxLevelNodeTypes` (:162), `TestCommonIntLeavesAndPassThrough` (:197), `TestCommonNeitherSubtree` (:235), `TestSynthesizedNodesAreCanonicalIntegers` (:255) — plus an extra `TestCommonExprAboveMaxInt32IsRangeViolation` (:304) added by the Deviation-5 fix commit. |
| 5 | Failure stats plumbing and run summary | DONE | `skill/stats.go` (82 lines): `Stats`, `Derivation`, `StatsAccumulator` with `Wrap`/`Total`/`Log`, matching the plan's Step 3 listing exactly (mutex-guarded, `Add` sums all six counters). `skill/reader.go` `Read` returns `model.Provider[Derivation]` (confirmed by diff). `skill/processor.go:17,41` `Register`/`RegisterSkill` both return `(Stats, error)`. `skill/mock/processor.go` mirrors the new signatures. `data/workers/skill.go:54-56` and `data/processor.go:153-155` both wire `skill.StatsAccumulator` — going beyond the design's single named call site, per approved Deviation 3. `skill/common_test.go:349` `TestCommonFailureIsScopedAndCounted` and `:433` `TestStatsAccumulatorSummary` both present and assert the exact field set (`tenant`, `jobId`, `skillId`, `key`, `expression`) plus the INFO/ERROR summary escalation. |
| 6 | atlas-ui — surface the new attributes | DONE | `services/atlas-ui/src/services/api/skills.service.ts:49,80-81` (`range?`, `mesoR?`, `consumeItemId?`, and the rest of the 35 — confirmed all present by grep) and `src/lib/skills/level-table.ts:56-88` (`["range","Range"]` … `["z","z"]`, unverified-meaning keys labeled with the bare key name as instructed, e.g. `["z","z"]` at :88). `npm run build` succeeds (see Build & Test Results) and `npx vitest run src/lib/skills` passes 23/23. |
| 7 | Archive verification — OQ-5 census + differential corpus | DONE | `docs/tasks/task-192-v95-skill-common-formulas/archive-census.md` (161 lines) censuses 10 archives (9 GMS + 1 JMS) exhaustively, confirms GMS v95.1 is the only archive with any `common` nodes (matching `wz-common-grammar.md`'s 954/319/633/2/635 counts independently), and reports a 14,711-evaluation differential check (this evaluator vs. a from-scratch naive-precedence reference) with 0 disagreements. `skill/formula/testdata/common_corpus.csv` has 14,712 lines (header + 14,711 rows), RFC-4180-quoted (confirmed the `" 375+5*x"` leading-space row exists at csv line 2). `skill/formula/corpus_test.go` (`TestArchiveCorpus`) matches the plan's listing verbatim and passes. |
| 8 | Gate sweep, e2e verification, code review | PARTIAL | Go gates (build/vet/test -race) verified clean by this audit (see below) — not previously recorded in `archive-census.md`, so the "Step 6: commit the verification record" close-out is effectively being done now via this audit rather than a dedicated prior commit. `tools/redis-key-guard.sh` and `tools/goroutine-guard.sh` both verified clean by this audit. `git diff --stat -- .../go.mod` is empty, correctly meaning `docker buildx bake atlas-data` was not required. Step 2 (merge main) was done — `f82f8305f` is exactly a merge of `origin/main` into the branch. Steps 3-4 (deploy, re-ingest, restart, live `GET` assertions) were **not performed** — explicitly approved (Deviation 4); reported as not-performed, not scored as a gap. Step 5 (code review) is this audit itself plus the parallel guideline reviewers, presumably dispatched alongside. See Build & Test Results for the one open item (`tools/lint.sh --check`, still running at report time). |

**Completion Rate:** 8/8 tasks implemented (Task 8's live-cluster half is an approved non-performance, not a failure) — 100% of producible work done.
**Skipped without approval:** 0
**Partial implementations:** 1 (Task 8 — gate-sweep recording gap only; substance verified in this audit)

### Skipped / Deferred Tasks

- **Task 8, Steps 3-4 (deploy branch image, `POST /data/process` re-ingest, restart serving pods, live `GET /api/data/skills/*` assertions):** not performed. This is **Deviation 4**, explicitly pre-approved by the human — do not treat as a gap. Impact: the fix is verified by unit/corpus tests and a full-archive differential census, but has not been confirmed against a live re-ingested v95 tenant. The archive-census.md does not carry a "Verification" section for this (as the plan's Step 4 asked), because that section documents live observed values which were never captured — consistent with the step not being performed.
- **Task 8, Step 1 recording:** the plan's Step 6 asks for a commit recording the gate-sweep + e2e verification notes appended to `archive-census.md`. No such commit exists on the branch (last commit affecting `archive-census.md` is `51bdc5492`, which is Task 7's corpus/census commit, not a distinct Task 8 verification commit). The gates themselves were, however, run and passed by this audit (see below) — the gap is procedural (missing commit / missing written record of the go build/vet/test/guard run), not a functional gap in the code.

### Build & Test Results

| Service / Module | Build | Tests | Notes |
|---|---|---|---|
| `services/atlas-data/atlas.com/data` (`go build ./...`) | PASS | — | Clean, no output. |
| `services/atlas-data/atlas.com/data` (`go vet ./...`) | — | PASS | Clean, no output. |
| `services/atlas-data/atlas.com/data` (`go test -race ./... -count=1`) | — | PASS | 65 packages, all `ok` (including `skill`, `skill/effect`, `skill/formula`); no failures, no `FAIL` lines. |
| repo root `tools/redis-key-guard.sh` | — | PASS | Exit 0; per-module log lines only (informational). |
| repo root `tools/goroutine-guard.sh` | — | PASS | Exit 0; per-module log lines only (informational). |
| repo root `tools/lint.sh --check` (`services/atlas-data` target) | — | PASS | Completed (exit reported per-target). Full run reported 5 failing targets tree-wide, none of them `services/atlas-data` — confirmed by grepping the full log for `atlas-data` (only stale cross-worktree cache warnings, no failure line). |
| `services/atlas-ui` (`npm run build`, Node 22) | PASS | — | Vite build succeeds; only pre-existing chunk-size warnings (`ConversationEditorPanel` 1.65 MB), unrelated to this change. |
| `services/atlas-ui` (`npx vitest run src/lib/skills`) | — | PASS | 4 test files, 23/23 tests passing. |

**`tools/lint.sh --check` detail:** the tree-wide run reported 5 failing targets: `atlas-merchant`, `atlas-monster-death`, `atlas-summons`, `atlas-tenants` (all pre-existing services untouched by this branch; each failed with `Error: parallel golangci-lint is running` — cross-worktree golangci-lint lock contention, a known project footgun, not a defect in this branch's code) and `ui:node-missing` (the background shell that ran `tools/lint.sh --check` did not have `nvm use 22` sourced; this audit's separate, dedicated `npm run build` and `npx vitest` runs, with Node 22 active, both succeeded — so this is an environment artifact of that one shell invocation, not a real atlas-ui lint failure). `services/atlas-data` — the only Go module this branch touches — is not among the 5 failing targets.

### Overall Assessment

- **Plan Adherence:** MOSTLY_COMPLETE (full on all producible work; Task 8's approved-skip live-cluster steps are the only open item, and they are an explicit non-blocker)
- **Recommendation:** READY_TO_MERGE from a plan-adherence standpoint. All Go and atlas-ui gates this branch is responsible for are clean (build, vet, race tests, both guard scripts, atlas-data lint, atlas-ui build+test). The tree-wide `tools/lint.sh --check` failures are pre-existing/unrelated services and lock contention, not this branch. Live re-ingest verification remains an explicitly deferred follow-up per the human's ruling (Deviation 4), not a merge blocker.

### Action Items

1. Optional/cleanup: land a small doc commit appending the Task 8 gate-sweep results (this audit's Build & Test Results table, or equivalent) to `archive-census.md` under a "Verification" heading, per the plan's Step 6 — currently only the Task 7 corpus/census content is committed there.
2. When the live-cluster half of Task 8 is eventually performed (deploy + re-ingest + restart + `GET` assertions), record the observed values in `archive-census.md` per the plan's Step 4 — tracked as a known, approved deferral (Deviation 4), not a blocker for this PR.
3. Not this branch's responsibility, but worth flagging upstream: `atlas-merchant`, `atlas-monster-death`, `atlas-summons`, and `atlas-tenants` are currently failing `tools/lint.sh --check` tree-wide with `Error: parallel golangci-lint is running` — likely cross-worktree lock contention rather than real lint violations in those services; re-run in isolation to confirm before treating as a real regression.
