# Plan Audit — task-225-evan-dragon

**Plan Path:** docs/tasks/task-225-evan-dragon/plan.md
**Audit Date:** 2026-08-13
**Branch:** task-225-evan-dragon
**Base Branch:** main (merge base `723519dc4`, head `f71298987`)

## Executive Summary

All 17 plan tasks are faithfully implemented with concrete, verifiable evidence. Every affected Go module builds, vets, and passes `-race` tests cleanly (`libs/atlas-packet`, `services/atlas-dragons/atlas.com/dragons`, `services/atlas-channel/atlas.com/channel`); all 8 project guards (`redis-key-guard`, `goroutine-guard`, `skill-job-id-guard`, `service-registration-guard`, `template-opcode-order-guard`, `template-duplicate-binding-guard`, `template-movement-types-guard`) pass clean; `packet-audit matrix --check` passes clean with all 24 dragon coverage-matrix cells (4 packets × 6 versions) at ✅ backed by evidence records; `docker buildx bake atlas-dragons` succeeds. The five pre-authorized deviations (version gate on SPAWN_DRAGON's jobId field, JOB_CHANGED split across services, dead `dragon.Extract` removal, gms_v84 fname canonicalization, Task 17 Steps 4–5 deferred) were all independently re-verified against source and are exactly as described — none constitute silent scope-narrowing. The only failing gate anywhere is `tools/lint.sh --check`'s `ui:node-version` target ("node v24 found, need v22"), which is an ambient Node-toolchain condition unrelated to any file this branch touched.

## Task Completion

| # | Task | Status | Evidence / Notes |
|---|------|--------|------------------|
| 1 | Dragon packet codecs | DONE | `libs/atlas-packet/dragon/clientbound/spawn.go`, `move.go`, `remove.go`, `libs/atlas-packet/dragon/serverbound/move.go` — all 4 codecs present with matching tests (`*_test.go`). `go build ./... && go test ./dragon/...` clean. int32 coords confirmed (spawn.go:48-49), discarded uint16 written unconditionally (spawn.go:86), serverbound Move has no leading identity field (serverbound/move.go:35 comment + Decode at :64-70). |
| 2 | `atlas-dragons` module scaffold | DONE | `services/atlas-dragons/atlas.com/dragons/{go.mod,main.go}` present; registered in `go.work:41`. `main.go` wires Redis registry, Kafka consumers, REST server — exceeds "minimal compiling main.go" because later tasks build atop the same scaffold in the same commit series (no separate minimal-then-expand step observed, which is a faithful compression, not a gap). |
| 3 | Dragon model, builder, `HasDragon` predicate | DONE | `dragon/model.go:39-56` — `HasDragon` resolves via `constants.For(...).Job.Resolve` (never a raw numeric range), checks `job.EvanStage1..EvanStage10`, excludes beginner 2001 by construction. `dragon/builder.go` present. `dragon/model_test.go` present, `go test ./dragon/...` passes. |
| 4 | Redis-backed dragon registry | DONE | `dragon/registry.go` — `storedDragon` uses `int32` X/Y (registry.go:35-39, explicitly diverging from atlas-summons' int16, matching Global Constraint). `dragon/registry_test.go` present (miniredis-based per plan). Guard `tools/redis-key-guard.sh` exit 0 — no raw go-redis keyed commands found outside `libs/atlas-redis`. |
| 5 | Character REST client | DONE | `character/rest.go`, `character/requests.go`, `character/processor.go`, `character/model.go` present. `character/rest.go:9-15` RestModel carries `job.Id`/x/y/stance per design. |
| 6 | Dragon Kafka contracts and event producer | DONE | `dragon/kafka.go` defines `EnvEventTopicDragonStatus`, `EventDragonStatus{Created,Moved,Destroyed}`. `dragon/producer.go` builds `createdEventProvider` etc keyed by map id (`producer.CreateKey(int(f.MapId()))`). `dragon/producer_test.go` present, passes. |
| 7 | Dragon processor — create, destroy, move | DONE | `dragon/processor.go` exposes `Create`, `Destroy`, `Move`, `GetByCharacterId`, `GetInField` (processor.go:45-155). `dragon/processor_test.go` present, passes under `-race`. |
| 8 | `atlas-dragons` REST surface | DONE | `dragon/rest.go:17` `InitResource`, `:29` `handleGetDragonByCharacterId`; `world/resource.go` for the `/worlds/{w}/channels/{c}/maps/{m}/instances/{i}/dragons` list route (self-review note #1's documented deviation from design §5.5, matching `atlas-summons` convention). Both nginx location blocks present in `deploy/k8s/base/routes.conf.template.generated` (`^/api/dragons` and the nested instances path). |
| 9 | `atlas-dragons` Kafka consumers | DONE | `kafka/consumer/dragon/consumer.go` handles CREATE/DESTROY/MOVE commands (`handleCreateCommand`, `handleDestroyCommand`, `handleMoveCommand`). `kafka/consumer/character/consumer.go` handles character-status cascade including `handleJobChanged` — confirmed it only owns the DESTROY half (consumer.go:52-73), matching the pre-authorized JOB_CHANGED split (deviation #2, plan Task 9 / Self-review note #2). |
| 10 | `atlas-channel` dragon contract mirror and client | DONE | `services/atlas-channel/atlas.com/channel/kafka/message/dragon/kafka.go` (87 lines) mirrors the atlas-dragons contract; `dragon/kafka_test.go` present and passes. `atlas-channel/atlas.com/channel/dragon/{model,processor,producer,requests}.go` present, providing the channel-side client (`dragoncmd.NewProcessor(...)`). |
| 11 | `atlas-channel` writers and serverbound move handler | DONE | `socket/writer/dragon.go` — `DragonSpawnBody`, `DragonMoveBody`, `DragonRemoveBody`. `socket/handler/dragon_move.go` — `DragonMoveHandleFunc` decodes serverbound Move and emits a MOVE command keyed on the session's character id (no identity field to trust/spoof, matches codec design). Registered in `main.go:661-663` (writer table) and `main.go:869` (`handlerMap[dragonsb.DragonMoveHandle]`). |
| 12 | `atlas-channel` dragon status consumer and JOB_CHANGED create | DONE | `kafka/consumer/dragon/consumer.go` (143 lines) — `excludesOwner` correctly excludes only MOVED broadcasts (consumer.go:29-32), `handles()` covers CREATED/MOVED/DESTROYED. `kafka/consumer/character/consumer.go:handleStatusEventJobChanged` (search hit) emits CREATE unconditionally using the live session's field, deferring the job-gate to atlas-dragons' `Processor.Create` — matches deviation #2 exactly, including the documented rationale (no map id on the event, `GET /characters/{id}` doesn't carry one either). |
| 13 | Replay existing dragons to entering session | DONE | `kafka/consumer/map/consumer.go:245` fans out `dragoncmd.NewProcessor(l, ctx).ForEachInMap(f, spawnDragonForSession(l)(ctx)(wp)(s))` inside `SpawnForSelf`, immediately after the summon fan-out (mirrors `spawnSummonForSession` at :239). `spawnDragonForSession` (consumer.go:464) skips `m.OwnerCharacterId() == s.CharacterId()` to avoid double-spawn with the CREATED broadcast, exactly as designed. Wrapped in `routine.Go` (goroutine-guard clean). |
| 14 | Seed template routing | DONE | Verified per-template: `template_gms_{83,84,87,92,95}_1.json` and `template_jms_185_1.json` each carry exactly 1 handler (`DragonMoveHandle`) + 3 writers (`DragonSpawn`, `DragonMove`, `DragonRemove`) = 24 entries total. Handler entries carry `validator` ("LoggedInValidator"), `fname` ("CVecCtrlDragon::EndUpdateActive"), and a non-empty `options.types` array (verified on gms_83 at line 1522-1540). Writer entries carry `fname` (e.g. "CDragon::OnCreated", "CDragon::OnMove", "CUser::OnDragonPacket"). `git diff --stat` over `services/atlas-configurations/seed-data/templates/` touches only these 6 files — the five legacy templates (`_12_`, `_48_`, `_61_`, `_72_`, `_79_`) are untouched, matching the Global Constraint. `tools/template-opcode-order-guard.sh`, `tools/template-duplicate-binding-guard.sh`, and `tools/template-movement-types-guard.sh` all exit 0. |
| 15 | `atlas-dragons` service registration | DONE | Cross-checked every hand-maintained list: `.github/config/services.json` (new entry), `docker-bake.hcl:51`, `go.work:41`, `deploy/k8s/base/atlas-dragons.yaml` (new Deployment+Service), `deploy/k8s/base/kustomization.yaml`, both overlays (`main`/`pr` kustomization + patches for `atlas-env-env.yaml`/`consumer-group-env.yaml`), `deploy/k8s/base/env-configmap.yaml` (COMMAND_TOPIC_DRAGON, EVENT_TOPIC_DRAGON_STATUS), nginx routes (`deploy/k8s/base/routes.conf.template.generated` + `deploy/shared/routes.conf`). Confirmed **no** database artifact anywhere: no diff to `tools/db-bootstrap.sh`, no `DB_NAME` env patch, no `ATLAS_DB_NAMES` entry, no migration files under the service dir. `tools/service-registration-guard.sh` exits 0 clean. |
| 16 | Promote the 24 coverage-matrix cells | DONE | `docs/packets/audits/STATUS.md` rows for SPAWN_DRAGON (line 309), MOVE_DRAGON clientbound (311), REMOVE_DRAGON (312), MOVE_DRAGON serverbound (741) each show ✅ across all six applicable version columns (gms_v83/84/87/92/95, jms_v185) = 24 ✅ cells. 24 matching evidence YAML files present under `docs/packets/evidence/{gms_v83,gms_v84,gms_v87,gms_v92,gms_v95,jms_v185}/dragon.*.yaml`. `go run ./tools/packet-audit matrix --check` exits 0 with no staleness warnings (only unrelated pre-existing n-a notes). |
| 17 | Full verification sweep and code review | DONE (Steps 1-3, 6-8) / DEFERRED-WITH-REASON (Steps 4-5) | Step 1 (build/vet/race): re-ran independently — all three modules clean. Step 2 (guards): all 7 project-specific guards + `tools/lint.sh --check` re-ran independently; only failure is `ui:node-version` ("node v24 found, need v22"), a pre-existing ambient condition, not a code defect — matches the controller's report verbatim. Step 3 (`docker buildx bake atlas-dragons`): re-ran independently, succeeds. Steps 4-5 (live tenant reconcile + live behavioral verification): confirmed NOT executed — `atlas-dragons` is not deployed anywhere and the branch is unpushed, so both are genuinely blocked by workflow sequencing rather than silently skipped. This is exactly the authorized deferral described in the audit brief; it should be handed to the user as an explicit post-deploy checklist rather than treated as "done." Step 6/7/8 (code review + audit.md): out of scope for this audit (concurrent reviewers are producing `audit.md`/other files separately per the dispatch instructions). |

**Completion Rate:** 17/17 tasks implemented (100%); 15/17 fully closed, 1/17 (Task 17) has two explicitly-authorized deferred sub-steps blocked on deployment, 0/17 unauthorized skips.
**Skipped without approval:** 0
**Partial implementations:** 1 (Task 17 — Steps 4-5 deferred with documented, pre-authorized reason; not a quality gap)

## Skipped / Deferred Tasks

- **Task 17, Steps 4-5 (live tenant socket-config reconcile; live behavioral verification).** Not executed. Reason given and independently corroborated: `atlas-dragons` is not deployed to any environment and the branch is unpushed, so there is no live tenant to reconcile against and no running client session to observe. This is a genuine external blocker (workflow sequencing — deploy happens post-merge), not a producible-but-deferred gap; there is no way to "just do it now" without first landing the branch. Impact: the dragon feature has full byte-level packet verification (24/24 matrix cells) and full unit/integration-level Go test coverage, but has not been observed working end-to-end against a live MapleStory client. This is the appropriate scope boundary for a pre-merge audit — the plan itself and the audit brief both call this out as a post-deploy checklist item for the user, not a silent omission.

## Build & Test Results

| Service | Build | Tests | Notes |
|---------|-------|-------|-------|
| libs/atlas-packet | PASS | PASS | `go build ./...` clean; `go test ./dragon/...` — both `clientbound` and `serverbound` packages pass. |
| atlas-dragons | PASS | PASS | `go build ./...`, `go vet ./...` clean; `go test -race ./... -count=1` — `atlas-dragons/dragon` passes (1.1s), all other packages report `[no test files]` (expected — thin REST/wiring layers). |
| atlas-channel | PASS | PASS | `go build ./...`, `go vet ./...` clean; `go test -race ./dragon/... ./kafka/... ./socket/... -count=1` — all packages with tests pass, including `kafka/message/dragon`, `kafka/consumer/dragon`, `socket/handler`, `socket/writer`. |
| Guards (redis-key, goroutine, skill-job-id, service-registration, template-opcode-order, template-duplicate-binding, template-movement-types) | N/A | PASS | All 7 exit 0. |
| tools/lint.sh --check | N/A | FAIL | Sole failure: `ui:node-version` ("node v24 found, need v22"). All Go golangci-lint targets report "0 issues." across the tree. This is an ambient toolchain condition (documented in project memory as a known false-fail pattern without nvm/correct Node on PATH) and not attributable to any file this branch touched. |
| docker buildx bake atlas-dragons | PASS | N/A | Image builds successfully; exports `atlas-dragons:local`. |
| packet-audit matrix --check | N/A | PASS | Exit 0, no staleness warnings for dragon rows; only unrelated pre-existing n-a evidence notes for CASHSHOP/teleport-rock. |

## Overall Assessment

- **Plan Adherence:** FULL
- **Recommendation:** READY_TO_MERGE (contingent on the user completing the Task 17 Steps 4-5 post-deploy checklist after this branch lands, per the plan's own sequencing)

## Action Items

1. After merge/deploy, reconcile every live tenant's socket configuration against the six updated templates (Task 17 Step 4) and confirm the four dragon bindings (`DragonMoveHandle` handler + `DragonSpawn`/`DragonMove`/`DragonRemove` writers) are present in the live config, not just the template file.
2. After reconciliation, run the Task 17 Step 5 live behavioral checklist (login spawn, second-player-sees-dragon-immediately, movement relay with no owner echo, map-change single-spawn/no-orphan, logout REMOVE_DRAGON, non-Evan/beginner exclusion) against a v84+ tenant using Loki with the `service_name` selector, and record actual observed values.
3. No code changes are required before merge; the `ui:node-version` lint failure is an environment condition, not a branch defect — resolve it locally (`nvm use 22`) rather than treating it as a blocking finding.
