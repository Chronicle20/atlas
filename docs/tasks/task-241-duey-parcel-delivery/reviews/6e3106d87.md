# Review: Task 28 batch 4/8 — gms_v84 PARCEL / DUEY_ACTION verification

Range reviewed: `2ebf0771a..6e3106d87` (substantive commit `6e3106d87`;
`d4b6e5811` is a docs/ledger-only commit — confirmed, see below — and is
passed over).

Brief: `.superpowers/sdd/plan/task-28-batch-gms_v84-brief.md`
Report: `.superpowers/sdd/plan/task-28-batch-gms_v84-report.md`

## Scope confirmation

`git diff --stat 2ebf0771a..6e3106d87` shows exactly the files the brief's
"Files" section names: 25 new `docs/packets/audits/gms_v84/Parcel*.{json,md}`
report pairs, 4 new `docs/packets/evidence/gms_v84/parcel.serverbound.*.yaml`,
one `docs/packets/ida-exports/gms_v84.json` splice, `STATUS.md`/`status.json`,
two new `v84_test.go` fixture files, and unrelated docs/ledger files (batch
1-3 review artifacts already landed earlier, plus this batch's own ledger
row). No other version's artifacts, no other family, no touched pre-existing
parcel test file. `d4b6e5811` (in-range) touches only
`agent-ledger.tsv`/`reviews/2ebf0771a.md` — confirmed docs-only, correctly
noted as pass-over material in the review brief.

## Findings

### 1. RULING 22 item-bearing fixtures — PASS

`libs/atlas-packet/parcel/clientbound/v84_test.go:16-45` defines
`wantEquipItemBytesV84()`, hand-built with an inline byte-by-byte comment
trail citing `GW_ItemSlotEquip::RawDecode`@`0x4eaf34` and the three
`Decode4` calls at `0x4eb134`/`0x4eb14e`/`0x4eb168`. Diffed against
`wantEquipItemBytesV83()` in `v83_test.go:24-44`, the only byte-level
difference is the inserted `0xff 0xff 0xff 0xff` nDurability field between
`experience` and `hammersApplied` — exactly matching the
`MajorAtLeast(84)` gate at `libs/atlas-packet/model/asset.go:260-262` and the
mirrored decode gate at `:605-607` (confirmed by direct read).

`TestParcelArrivedV84WithItem` (`v84_test.go:186-210`) and
`TestParcelOpenV84WithItem` (`v84_test.go:260-283`) both attach a real item
via `.SetItem(item)`, assert `p.Encode(...)` and the enclosing
`NewParcelArrived`/`NewParcelOpen` wrapper against the full hand-built byte
sequence including `wantEquipItemBytesV84()` — not merely a re-assertion of
the notice-only path. `go test -count=1 ./libs/atlas-packet/parcel/...`
passes (`ok` both packages).

### 2. Expected bytes grounded in pinned evidence, not the encoder's own output — PASS

All clientbound fixtures build `want`/`pBytes` from literal byte slices with
inline provenance comments (mode byte, StringPool notice-only single byte,
234-byte fixed block layout, hand-derived item bytes) rather than calling the
encoder and asserting `got == got`. Serverbound fixtures use the pre-existing
`sendNpcBytes()`/`sendQuickBytes()` fixed byte helpers
(`action_test.go:135-171`, shared and unmodified since batch 1) round-tripped
through decode→encode — the same pattern already accepted in the v72/v79/v83
reviews.

### 3. Marker-address fix — PASS, verified across all 25, not just the 2 named

Extracted all 25 `packet-audit:verify ... ida=0x...` markers from
`libs/atlas-packet/parcel/{clientbound,serverbound}/v84_test.go` and
cross-checked each against the `Address` field of its corresponding
`docs/packets/audits/gms_v84/Parcel*.json` report by script. Result: 25/25
exact matches, zero mismatches, zero orphan markers. Independently re-ran
`go run ./tools/packet-audit matrix --check` — exits 0, no orphan-marker
errors. The report's claim that the fix (retargeting
`ParcelSendEnableActions`/`ParcelRecvEnableActions` from the generic
`0x70dc07` default-arm address to `0x70e0a6`/`0x70e162`) was applied and
re-verified against all 25 addresses holds; both entries were found matching
correctly in the current tree.

### 4. Evidence hashes — PASS, verified as real (not placeholder)

`git diff --numstat` on `docs/packets/evidence/` for this range shows 4 new
files, 40 insertions, 0 deletions — exactly the four DUEY_ACTION arms,
matching RULING A (PARCEL clientbound not pinned). Built a temporary
in-tree tool (`tools/packet-audit/cmd/hashtool_review`, same technique the
report describes — a throwaway `internal/evidence`-importing binary inside
the module tree) and computed `evidence.FunctionHash` directly against the
committed `docs/packets/ida-exports/gms_v84.json` for all four fnames.
Results matched the committed YAML `decompile_sha256` values byte-for-byte:

```
CParcelDlg::CloseParcelDlg 21eca5da844f28ffd2fcac75eb603438b0d8e3fb8f47d37b5dbd114d6a3d1cfb
CTabReceive::DiscardParcel e448789eccc8f8f3796fde02b7afe40395097bb38c461fc46a6842682b30e0f7
CTabReceive::ReceiveParcel 271c69659f394bf504b80bf583131c9039c837e06533a57b5a04ce7f4d87be31
CTabSend::SendParcel      1b1f3257e9d3d3e4236673a69b171275dc799ce28c947e0e6a17a52bb1afbe3a
```

All 64-hex-char (valid sha256). All 4 `verifies:` fields point at test
functions that genuinely exist (`TestActionCloseV84`,
`TestActionSendV84`, `TestActionReceiveV84`, `TestActionDiscardV84` in
`v84_test.go`). My temporary tool was removed after use;
`git status --porcelain` confirms the tree has no stray tool directory or
build artifact.

### 5. Scope discipline — PASS

- `git diff --numstat` on `docs/packets/ida-exports/gms_v84.json` →
  `346  0` — matches the report's claim and batch 1's precedent exactly;
  JSON parses cleanly.
- `docs/packets/audits/STATUS.md` diff is confined to: the `gms_v84` export
  hash line, the `PARCEL` row's `0x149` cell (❌→✅), the `DUEY_ACTION` row's
  `0x041` cell (❌→✅), the three `ParcelAction{Discard,Receive,Send}` v84
  summary-table cells, and the v84 summary-line counters (mechanical fallout
  of promotion). No other row or version touched.
- `docs/packets/audits/status.json` diff: export hash line + the 5 expected
  `gms_v84` cell transitions (`DUEY_ACTION`, `PARCEL`,
  `ParcelActionClose`/`Discard`/`Send`), each `incomplete`→`verified`,
  6 insertions / 11 deletions, nothing else touched.

### Gate commands — re-run independently, all exit 0

```
go test -count=1 ./libs/atlas-packet/parcel/...        -> ok (both packages)
go run ./tools/packet-audit matrix --check              -> exit 0, clean
go run ./tools/packet-audit dispatcher-lint              -> "dispatcher-lint: clean"
go run ./tools/packet-audit fname-doc --check             -> "fname-doc check OK"
go run ./tools/packet-audit operations --check            -> "operations check OK"
```

### RULING C fixture pattern — PASS

Two new per-version files (`v84_test.go` clientbound and serverbound), one
each, stacked markers over table-driven and per-arm tests, matching the
batch-1 anchor pattern. `git diff --stat` for `libs/atlas-packet/parcel/`
shows only the two new files — the four pre-existing parcel test files were
not modified.

### Minor observation (non-blocking)

The report's SEND-address decompile note (two independent client call sites
sharing opcode 0x41, NPC path @`0x70bb61` vs quick path @`0x70a2ae`) is a new
finding not present in the v83 anchor's narrative; the marker/evidence/test
all consistently cite the quick-send address `0x70a2ae`, and the codec
(`ActionSend`) already handles both shapes via the existing
`action_test.go` helpers, so nothing here required a wire fix. Documented for
completeness, not a defect.

## Not evaluable

None. The unit's full review surface (fixtures, markers, evidence, export
splice, STATUS.md/status.json, gate commands) was independently
re-verified against the working tree, not taken on the report's word alone.

## Verdict rationale

Every claim in the implementer's report was independently checked against
either the working tree or a freshly re-run tool invocation, and all held.
No blocking or non-blocking defects found.
