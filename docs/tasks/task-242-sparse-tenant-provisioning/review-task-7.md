# Review: Task 7 — Document the boundary in the runbook

**Commit range:** `3ac3d2b46..c5a299163` (single commit `c5a299163`)
**Brief:** `.superpowers/sdd/plan/task-7-brief.md`
**Report:** `.superpowers/sdd/plan/task-7-report.md`
**File changed:** `docs/runbooks/ephemeral-pr-deployments.md` (+82/-0, single file)

## Scope

This is a documentation-only unit: one new section, `## §9.16 Sparse-mode
tenant ownership and the environment boundary`, appended after `## §9.15`.
Review method: fact-check every path, line number, identifier, metric name,
log string, and command cited in the new section against the actual source
in the tree at `c5a299163`.

## Requirement-by-requirement (brief's six items, in order)

1. **What a sparse environment owns** — PASS. Cites `tenant-create`
   (`bootstrap.sh:320`) and `record-tenant` (`bootstrap.sh:363`, log line
   `recorded tenant=$TENANT_ID on environment record $ATLAS_ENVIRONMENT`).
   Both line numbers and the log-line template verified exact:
   `grep -n "ATLAS_STEP=tenant-create"` → `320:ATLAS_STEP=tenant-create`;
   `grep -n "recorded tenant="` → `363:...recorded tenant=$TENANT_ID on
   environment record $ATLAS_ENVIRONMENT`. `find_environment_tenant` /
   `create_environment_tenant` confirmed defined at `bootstrap.sh:74` and
   `:87`. The `(region, majorVersion, minorVersion)` triple-sharing claim
   matches the read of `bootstrap.sh:320-330` (looks up/creates against
   `canonical_region`/`canonical_major`/`canonical_minor`).

2. **What it does not duplicate** — PASS, and the implementer's two
   corrections against the brief both check out:
   - Data-ingest guard location: doc cites `bootstrap.sh:602`. Confirmed —
     `grep -n "^# Data ingest"` → `602:# Data ingest: baseline-restore
     only.` The brief's own line (541) is stale; 602 is correct on this
     tree. Correcting the brief was the right call, not a new error.
   - Document count: doc says "~49k canonical rows." Confirmed against the
     comment at `bootstrap.sh:~617` ("already held all ~49k canonical
     rows"), and independently against commit `c5e88320a`'s message ("held
     all ~49k of them"). The brief's "~48k" was wrong; ~49k is grounded.
   - `c5e88320a` commit-hash citation confirmed: `git show --stat
     c5e88320a -- services/atlas-pr-bootstrap/scripts/bootstrap.sh` returns
     exactly the commit titled "fix(pr-bootstrap): stop the data-ingest
     guard restoring into a populated database (#1423)".
   - "0 owned documents" / canonical-fallback-via-`document/storage.go`
     framing matches the guard's own comment block (lines 601-617), which
     states the same mechanism in the same terms.

3. **Confirm-the-boundary-held checklist** — PASS. All four brief-specified
   checklist items present (non-empty distinct `tenant`; `<N>.atlas.home`
   returns exactly one; `dev.atlas.home` returns only main's; the
   `recorded tenant=<id> on environment record pr-<N>` log line). The
   implementer additionally folded in `tools/overlay-env-guard.sh` as the
   automated counterpart, as required by the brief's Task-6 addendum note.
   Verified: the script exists at `tools/overlay-env-guard.sh`, its header
   describes exactly what the doc claims (pins per-overlay
   `ATLAS_ENVIRONMENT` literal and the ingress `ATLAS_ENVIRONMENT_DEFAULT`
   wiring), and it is wired into `tools/verify.sh`'s `deploy/`-touched
   block: `grep -n overlay-env-guard tools/verify.sh` →
   `554:if touched '^(deploy/|tools/gen-lb-ports\.sh|tools/overlay-env-guard\.sh|.*versions\.json)'` /
   `558:    step "overlay env drift"   ./tools/overlay-env-guard.sh`. The
   doc's claim that it "fails `tools/verify.sh`'s `deploy/` block" is
   accurate.

4. **The two Kafka gate metrics** — PASS. Both names grepped verbatim in
   `libs/atlas-kafka/consumer/gate.go`:
   `atlas_kafka_gate_skipped_not_owner_total` at line 30,
   `atlas_kafka_gate_dropped_unresolvable_total` at line 38 — exactly the
   line numbers the doc cites (`:30` and `:38`).

5. **`main` prerequisite and bring-up window** — PASS. Sync-wave 11
   confirmed literally in `deploy/k8s/overlays/main/environment-record.yaml`
   (`argocd.argoproj.io/sync-wave: "11"`). `ATLAS_ENVIRONMENT=main`
   confirmed in `deploy/k8s/overlays/main/kustomization.yaml:48`. The
   "atlas-ingress-configmap is a plain resource, not generated by
   kustomize" claim checked two ways: the ConfigMap is a literal
   `kind: ConfigMap` object in `deploy/k8s/base/atlas-ingress.yaml` (not
   produced by a `configMapGenerator` block), and `deploy/k8s/base/
   kustomization.yaml`'s `configMapGenerator` list only contains
   `atlas-ingress-routes`, not `atlas-ingress-configmap` — the doc's
   "editing `nginx.conf` does not roll the pods on its own" reasoning
   (no generator → no content-hash suffix → no automatic pod restart) is
   correct kustomize behavior for a plain resource.

6. **Regression pin pointer** — PASS. `TestTemplatesFallBackToTheBaselineRow`
   confirmed at `services/atlas-configurations/atlas.com/configurations/
   templates/overlay_test.go:65` exactly as cited.

## Conventions

- **No absolute/home paths**: `git diff 3ac3d2b46..c5a299163 -- docs/
  runbooks/ephemeral-pr-deployments.md | grep -nE "/home/|/root/|/Users/"`
  → no matches. All backtick-quoted absolute-looking paths in the added
  lines are repo-relative (`services/...`, `libs/...`, `deploy/...`).
- **Line endings**: `grep -c $'\r' docs/runbooks/ephemeral-pr-deployments.md`
  → 0; file is LF throughout, consistent with a pure append.
- **Heading numbering**: `grep -n "^## §9\."` over the full file shows
  §9.1 through §9.15 followed immediately by the new §9.16 — no gaps, no
  collisions, consistent with the existing sequence.
- **Diff shape**: `git diff --stat` confirms a single-file, insertion-only
  diff (82 lines added, 0 removed), matching the report's claim and the
  brief's single-file scope.

## Not evaluable

None. Every claim in the new section cites a file, line, identifier, or
command that could be checked directly against the tree at this commit,
and all were checked.

## Verdict rationale

Every one of the six required subsections is present, in the brief's
order, and every fact, path, line number, metric name, log string, and
command cited in them is verified correct against the current tree —
including both of the implementer's deviations from the brief's own
(stale) line number and document-count figure, which are the more
scrupulous facts and check out. No convention violations found.
