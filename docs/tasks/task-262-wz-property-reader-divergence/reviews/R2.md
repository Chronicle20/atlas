# Review: Task R2 — whole-archive size-accounting self-check

Range reviewed: `0b3c8b21c..8c86277` (1 commit, 6 files, +401/-15)
Brief: `.superpowers/sdd/plan/task-R2-brief.md`
Report: `.superpowers/sdd/plan/task-R2-report.md`

## Scope confirmation

The diff matches the six files named in the brief exactly, no more, no less:
`wz/trace.go`, `wz/image.go`, `wz/trace_test.go`, `wzdiff/selfcheck.go` (new),
`wzdiff/selfcheck_test.go` (new), `cmd/wzdiff/main.go`. `go.work.sum` was left
untouched as instructed (`git show 8c86277 --stat -- go.work.sum` returns
nothing). Scope confirmed: matches brief.

## Findings by brief requirement

### 1. `TraceEvent.DeclaredEnd`/`ActualEnd` (Step 1)

`wz/trace.go:29-34` adds the two `int64` fields with the doc comment
specified in the brief (zero-for-non-sub contract stated). `wz/image.go:355-370`
populates them from the already-in-scope `endPos`/`actualEnd` locals at the
existing `Kind: "sub"` emit site — the diff shows only field additions to the
struct literal, no other line touched (confirmed via `git diff` field-by-field).
`Detail`'s format string is byte-identical (`fmt.Sprintf("declaredSize=%d
endPos=%d actualEnd=%d", size, endPos, actualEnd)` unchanged).

The nil-hook path is unaffected: `if hook != nil { ... }` still guards the
`r.Pos()` call that captures `actualEnd`, so the "no syscall on nil hook"
comment at `wz/image.go` remains true — the new fields are populated only
inside that existing guarded block. PASS.

`wz/trace_test.go` extends `TestSetTraceEmitsNodeEvents` with a loop asserting
zero `DeclaredEnd`/`ActualEnd` on every non-`sub` event, and adds
`TestSetTraceEmitsSubEventDeclaredActualEnd`, which asserts both fields are
non-zero, equal (well-formed fixture), and reconstructs the exact `Detail`
string from the structured fields to prove `Detail` truly wasn't touched
(`subEvent.DeclaredEnd-subEvent.StartOff-4` correctly recovers `size` from
the geometry: `start` is captured right after the type-9 tag byte, the next
4 bytes are the length field, so `endPos - (start+4) == size`). PASS.

### 2. `wzdiff/selfcheck.go` — the gate itself (Step 2)

`SelfCheck` (`wzdiff/selfcheck.go:54-97`) opens the archive, calls
`f.SetTrace` before the `Properties()` loop begins (line 67, loop starts at
line 89) — satisfies the "before any `Properties()` call" half of the
concurrency contract. The hook (lines 67-81) only appends to
`result.Violations` and increments `result.SubObjects`, reading a
closed-over `currentImage` string that is set by the same (single) goroutine
immediately before each `Properties()` call — no I/O, no re-entrant reader
call. PASS on both concurrency-contract items in the review brief (#4).

Per-image parse errors are appended to `ParseErrors` and the walk continues
(`selfcheck.go:91-93`); the returned `error` is reserved for the archive-open
failure only (`selfcheck.go:55-58`). Confirmed from code, not the report, per
the review brief's instruction. PASS on item #3.

### 3. Tests (Step 4)

- `TestSelfCheckCleanArchivePasses` (`wzdiff/selfcheck_test.go:19-53`) builds
  a one-sub-object archive and explicitly asserts `result.SubObjects == 0` as
  a failure condition (line 41-43), independent of the zero-violations
  assertion — this is the load-bearing "gate that walks nothing trivially
  reports clean" guard the review brief calls out as item #2. PASS.
- `TestSelfCheckCorruptedArchiveFails` (`wzdiff/selfcheck_test.go:68-126`)
  patches the serialized declared-size int32 by locating its file offset via
  a clean trace pass (`traceSubEvent`), then asserts `len(Violations) == 1`
  and the violation's `Image` (`"0200"`), `Path` (`"/info"`), `DeclaredEnd`
  (`subEvent.DeclaredEnd - 1`, i.e. the exact corrupted value) and
  `ActualEnd` (`subEvent.ActualEnd`, i.e. unaffected by the corruption) — all
  four fields against concrete expected values, not merely non-empty. This is
  the load-bearing test called out as item #1 in the review brief, and it
  would genuinely fail against a broken gate (e.g. one that didn't wire
  `DeclaredEnd`/`ActualEnd` through, or compared the wrong fields) rather than
  merely asserting non-emptiness. PASS.
- Both tests reuse existing `wzdiff` test helpers (`testLogger`,
  `writeArchive` from `run_test.go`) and `wztest.NewBuilder()` — no
  `*_testhelpers.go`-style test-only constructors introduced. Consistent with
  the Builder-pattern convention.

### 4. CLI wiring (Step 3)

`cmd/wzdiff/main.go:59-69` adds the `--selfcheck` dispatch arm before the
`--reference` requirement check, mirroring the existing `--trace` arm exactly
in shape; exit 0 only when `len(Violations) == 0 && len(ParseErrors) == 0`,
exit 1 otherwise — matches the package's exit-code convention. The package
doc comment (`main.go:1-19`) was updated to stop attributing the dump format
exclusively to HaRepacker and to document the new mode. PASS.

## Verification run myself

```
$ cd libs/atlas-wz && GOCACHE=/tmp/gocache-262 go build ./... && GOCACHE=/tmp/gocache-262 go test ./...
ok  	.../atlas-wz/wz	(cached)
ok  	.../atlas-wz/wzdiff	(cached)
... (all packages ok, no failures)
```

Real-archive corroboration (path used for verification only, not committed):

```
$ GOCACHE=/tmp/gocache-262 go run ./cmd/wzdiff --selfcheck --archive <local-path>/Reactor.wz
images: 419  sub-objects: 15428  violations: 0  parse errors: 0
```

Matches the report's claimed numbers exactly. Per the controller-resolved
context, the 15428-vs-1136 discrepancy against Task 5's hand-run is not a
defect (1136 was scoped to 19 divergent images, not the full 419-image
archive) — not re-litigated here.

## Repo constraints

- No literal absolute/home paths in any committed file
  (`grep -rn "/home/" <six changed files>` returns nothing).
- No new domain constant/type/alias introduced; `libs/atlas-constants/` check
  not applicable to this diagnostic package.
- Builder pattern used throughout tests (`wztest.NewBuilder()`), no
  `*_testhelpers.go`.
- `go.work.sum` untouched, consistent with "stage the six files by name"
  instruction.

## Not evaluable

- The correctness of `File.parent`/`traceHook` delegation for sub-file views
  (`wz/file.go`, `wz/trace.go:59-66`) was not re-verified here: it is
  pre-existing infrastructure this diff does not touch, already exercised by
  `wzdiff.Trace` (the pattern this task's brief explicitly said to copy) and
  by existing sub-file tests. Correctness of that delegation for the
  multi-image walk in `SelfCheck` rests on that pre-existing contract holding,
  which is outside this diff's review surface.

## Verdict

No blocking findings. All five weighted review-brief items pass with
file:line evidence; build and tests are clean; real-archive numbers match the
report verbatim.
