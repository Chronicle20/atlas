# Review: Task R3 — scope the `1136` figure, close two deferred minors

Commit range: `8e1f7201f..HEAD` (`90994bd2a`, `36882bf08`)
Brief: `.superpowers/sdd/plan/task-R3-brief.md`
Report: `.superpowers/sdd/plan/task-R3-report.md`

## Scope confirmed

Reviewed the two commits in full: five docs edits (R3.1 scope-of-`1136`
citations + R3.3 blast-radius note) and the `adapter.go`/`adapter_test.go`
pair (R3.2). This matches the brief's file list exactly; no unrelated files
touched. `git diff --stat 8e1f7201f..HEAD` shows only the 7 files listed in
the brief/report.

## Check 1 — every `1136` is scoped; `15428`/`419`/`0`/`0` traces to R2

`grep -rn '1136' docs/tasks/task-262-wz-property-reader-divergence/` (excluding
the `reviews/` directory, which is review output, not task-doc prose):

- `design.md:149` — "1136 type-9 sub-objects traced across the **19
  divergent images**" — scoped.
- `prd.md:358` — "...took across 1136 sub-objects in the **19 divergent
  images**" — scoped.
- `plan.md:1020` — "1136/1136 type-9 sub-objects traced clean across the **19
  divergent images**" — scoped.
- `plan.md:1264` — "Task 5 already ran this check by hand across the **19
  divergent images** and found 1136/1136 sub-objects clean" — scoped. (Note:
  the brief cited this site as `:1261`; it landed three lines later at
  `:1264` after the R3.1 edits shifted line numbers earlier in the file —
  cosmetic, not a defect, the content is correct.)
- `provenance.md:79` — "1136 type-9 sub-objects traced across the **19
  divergent images**" — scoped.
- `reference-fidelity.md:60` — "of all 19 divergent images there are **1136
  type-9 sub-objects...**" — scoped (this was already correct pre-R3, per the
  brief).
- `reference-fidelity.md:68` — the literal `1136` inside a quoted shell
  command's output (`grep -c 'kind=sub'` → `1136`) — verbatim evidence, not a
  prose claim; correctly left unscoped by the report's own reasoning, and the
  surrounding paragraph (`:60`) already names the scope.
- `reference-fidelity.md:72` — "...mis-size it by exactly zero bytes 1136
  times in a row..." — sits directly under the `:60` paragraph that names the
  19-image scope; acceptable in context.
- `reference-fidelity.md:86` — "The two figures — 1136 across the 19 divergent
  images, 15428 across the full 419-image archive — measure different
  scopes..." — scoped, and explicitly declines to reconcile them, matching
  the brief's "do not invent a reconciliation" instruction.

No occurrence reads as an unscoped whole-archive claim. All sites the brief
named are addressed.

`15428` sites: `design.md:151`, `prd.md:360`, `plan.md:1022`,
`provenance.md:81`, `reference-fidelity.md:82,85,86` — every one is attributed
to "Task R2's ... whole-archive self-check" and/or "all 419 images in
`$WZ_ARCHIVE`". Verified the verbatim source:

```
$ sed -n '105,125p' .superpowers/sdd/plan/task-R2-report.md
...
$ go run ./cmd/wzdiff --selfcheck --archive .../83.1_wz/Reactor.wz
...
images: 419  sub-objects: 15428  violations: 0  parse errors: 0
```

`reference-fidelity.md:82` quotes this line verbatim, character for
character. No figure was recomputed or invented — both `1136` (pre-existing,
untouched wording) and `15428`/`419`/`0`/`0` (quoted from
`task-R2-report.md:113-119`) trace to their named sources.

**PASS.**

## Check 2 — `adapter.go:41` gates on `failed > 0`; one summary line either branch; no per-property granularity

`services/atlas-data/atlas.com/data/data/wztoxml/adapter.go:42-46`:

```go
if failed > 0 {
    l.Warnf("wz archive [%s]: %d of %d images failed to serialize", archiveName, failed, total)
} else {
    l.Infof("wz archive [%s]: %d of %d images failed to serialize", archiveName, failed, total)
}
```

- Gate is exactly `failed > 0`, matching the brief.
- Identical message string on both branches; only the log level and method
  differ. Exactly one summary line is emitted per call (this code path runs
  once per `SerializeToDirectory` call, i.e. once per archive) regardless of
  branch — no new call sites, no loop over properties.
- `serializeDirectory`'s doc comment (`adapter.go:53-54`) still asserts "It
  never aggregates per-property detail" and the code doesn't touch that path;
  the fix is confined to the summary line's level selection.

Test honesty — traced both branches by hand, did not accept the asserted
RED/GREEN at face value:

- `TestSerializeDirectorySuccessLogsInfo` (`adapter_test.go`, one
  fully-successful image) asserts the "0 of 1 images failed to serialize"
  line contains `level=info` and never `level=warning`. Under the
  **pre-fix** code (`l.Warnf(...)` unconditional, confirmed via `git show
  8e1f7201f:.../adapter.go`), this summary line would always render
  `level=warning`, so this specific assertion is unsatisfiable pre-fix — a
  genuine RED-then-GREEN test, not one that passes either way.
- `TestSerializeDirectoryCountsFailures` (extended) asserts the "1 of 2
  images failed to serialize" line contains `level=warning`. This assertion
  would already have passed under the pre-fix code (which always warns), so
  by itself it does not discriminate the fix — but it correctly pins the
  failed-branch behavior going forward, and the brief only required "if a
  test asserts on the log level or message, update it to cover both
  branches" (satisfied jointly with the new Success test).
- Both tests also assert per-image and summary line counts (`perImageLines
  != 1`, `summaryLines != 1`) and that no property name (`"price"`) leaks
  into the log, holding the "no per-property granularity" constraint.
- Ran the tests directly (not just trusting the report):

  ```
  cd services/atlas-data/atlas.com/data
  GOCACHE=/tmp/gocache-262 go build ./...
  GOCACHE=/tmp/gocache-262 go test ./data/wztoxml/... -v
  ```

  Output: `TestRoundTripImage`, `TestSerializeDirectoryCountsFailures`,
  `TestSerializeDirectorySuccessLogsInfo` all `PASS`; `go build ./...` clean.

**PASS.**

## Check 3 — `plan.md:1130` blast-radius note; `wzcache.go` unchanged

`plan.md:1130` (the "Blast-radius note, already surveyed" paragraph) now
reads:

> `services/atlas-renders/atlas.com/renders/storage/wzcache.go:135`
> (confirmed safe by the Task 14 reviewer: it checks the error, removes the
> partial download, and never touches a partial file)

This matches the brief's required phrasing and attribution (Task 14
reviewer, not this task's own assertion).

Confirmed `wzcache.go` itself has zero diff in this range:

```
$ git diff 8e1f7201f..HEAD -- services/atlas-renders/atlas.com/renders/storage/wzcache.go
(empty)
```

**PASS.**

## `go.work.sum`

```
$ git show 90994bd2a --stat   # 5 docs files only
$ git show 36882bf08 --stat   # adapter.go + adapter_test.go only
```

`go.work.sum` appears in neither commit's stat. **Not staged — confirmed.**

## Repo conventions

- No literal home/absolute path introduced under `docs/`: `grep -n
  "/home/tumidanski"` across the five edited docs returned nothing.
- Both commits are scoped, single-purpose, conventional-commit-style
  messages matching repo convention.

## Not evaluable

- Whether the whole-archive self-check figures (`419`/`15428`/`0`/`0`)
  themselves are correct is out of scope — per the brief, neither WZ archive
  is reachable from this environment, and R3's job was only to cite them
  faithfully, which it did.
- `wzcache.go:135`'s claimed safety (checks error, removes partial download,
  never touches partial file) was asserted by the Task 14 reviewer, not
  re-verified here — R3's brief marks `wzcache.go` read-only/out of scope for
  this unit, and the brief only asks that the doc *say* it was confirmed
  safe, which it accurately does.

## Verdict

All three specifically-requested checks pass with cited evidence; no
additional defects found in the diff. This is the final unit on the branch.

APPROVED
