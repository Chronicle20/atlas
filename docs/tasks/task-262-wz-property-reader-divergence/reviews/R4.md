# Review: Task R4 — DOM-20 table-drive triage

Commit range: `36882bf08..HEAD` (HEAD = `ca8b7980a`).
Brief: `.superpowers/sdd/plan/task-R4-brief.md`.
Report: `.superpowers/sdd/plan/task-R4-report.md`.

## Scope confirmed

`git diff --stat 36882bf08..HEAD`:

```
docs/tasks/task-262-wz-property-reader-divergence/dom20-dispositions.md | 57 ++++
libs/atlas-wz/wz/wztest_kinds_test.go                                   | 124 ++++---
libs/atlas-wz/wzdiff/allowlist_test.go                                  | 355 +++++++++------------
3 files changed, 285 insertions(+), 251 deletions(-)
```

Only test files and the new disposition doc changed. No non-test file was
touched (`directory.go`, `file.go`, `reader.go`, `wztoxml/adapter.go` are
untouched, confirmed via the diff stat and manual grep). Matches the brief's
"convert the two called-out findings, triage the rest" scope exactly —
findings 1–4 and 7–10 were all left flat, findings 5 and 6 converted. No
scope mismatch.

## Coverage verification (mechanical, not narrative)

### Finding 5 — `wztest_kinds_test.go`

`git show 36882bf08:libs/atlas-wz/wz/wztest_kinds_test.go` vs. the working
tree: the original `TestBuilderEmitsAllPropertyKinds` body (lines 17–100 of
the pre-image) checks 10 property kinds inline — `null`, `short`, `int`,
`long`, `float`, `double`, `string`(`str`), `vector`(`lt`), `uol`, `convex`.
Diffed line-by-line against the post-image (lines 48–115): every
`t.Fatalf` message, comparison operator, and expected literal
(`int16(-3)`, `int32(42)`, `int64(9000000000)`, `float32(1.5)`,
`float64(2.25)`, `"hello"`, `(-100,-100)`, `"../0/0"`, the two convex
children `(1,2)`/`(3,4)`) is byte-identical, just moved into a
`{kind string; check func(...)}` row and a `t.Run(tc.kind, ...)` wrapper.
`TestBuilderFloatZeroWithoutMarker` and `TestBuilderWzIntAndLongBoundaries`
are untouched (confirmed identical in both blobs).

Ran the converted test directly:

```
GOCACHE=/tmp/gocache-262 go test ./wz/... -run TestBuilderEmitsAllPropertyKinds -v -count=1
```

produced exactly 10 `t.Run` subtests — `null, short, int, long, float,
double, string, vector, uol, convex` — all PASS. Matches the 10 kinds in
the pre-image 1:1. No scenario dropped, no assertion weakened.

### Finding 6 — `allowlist_test.go`

`git show 36882bf08:libs/atlas-wz/wzdiff/allowlist_test.go` has 11 functions:
`TestLoadAllowlist`, `TestLoadAllowlistMalformedLine`,
`TestLoadAllowlistRejectsBlankImage`, `TestLoadAllowlistRejectsBlankPath`,
`TestLoadAllowlistRejectsBlankOnlyIn`, `TestLoadAllowlistRejectsInvalidOnlyIn`,
`TestLoadAllowlistNormalizesTrailingSlashOnPath`,
`TestLoadAllowlistNormalizesWhitespace`,
`TestLoadAllowlistRejectsBareSlashPath`,
`TestLoadAllowlistRejectsRepeatedSlashPath`,
`TestLoadAllowlistNormalizesRepeatedTrailingSlashOnPath`, plus the
already-table `TestAllowed` (5 pre-existing subtests, untouched). That's
exactly 10 flat functions + 1 pre-existing table, matching the report's
claim.

Mapped each of the 10 flat functions to a row in the post-image:

- `TestLoadAllowlist` → `TestLoadAllowlistParsesAndNormalizes/basic_two_entries`
  — same content string, same two `AllowEntry` want values, same
  `len(entries) != 2` check.
- `TestLoadAllowlistNormalizesTrailingSlashOnPath` →
  `.../normalizes_trailing_slash_on_path` — same content, same expected
  `Path == "/imgdir:0"`, same `Allowed(...)` round-trip check
  (`checkAllowed: true`).
- `TestLoadAllowlistNormalizesWhitespace` → `.../normalizes_whitespace` —
  same content, same want entry, same `Allowed` round-trip.
- `TestLoadAllowlistNormalizesRepeatedTrailingSlashOnPath` →
  `.../normalizes_repeated_trailing_slash_on_path` — same content, same
  want, same `Allowed` round-trip.
- `TestLoadAllowlistMalformedLine` →
  `TestLoadAllowlistRejectsInvalid/malformed_line` — same content,
  `wantErrSubstr: ":2:"` preserves the original's
  `strings.Contains(err.Error(), ":2:")` check exactly.
- `TestLoadAllowlistRejectsBlankImage/BlankPath/BlankOnlyIn/InvalidOnlyIn/
  BareSlashPath/RepeatedSlashPath` → the remaining 6 rows in
  `TestLoadAllowlistRejectsInvalid` — each row's `wantErrSubstr` is
  deliberately left empty (`""`) because **the originals never asserted an
  error string for these six cases either**, only `err == nil` — confirmed
  by reading the pre-image (lines 61–65, 78–81, 91–94, 104–107, 171–174,
  189–192 of `/tmp/before_allow.go`: every one of these six is a bare
  `if err == nil { t.Fatal(...) }`, no `strings.Contains`). The
  `if c.wantErrSubstr != "" && ...` guard in the post-image is therefore
  not the classic "empty-field masks a dropped assertion" bug — it
  faithfully reproduces the fact that these six originals asserted
  presence-of-error only, nothing more.

Ran:

```
GOCACHE=/tmp/gocache-262 go test ./wzdiff/... -run 'TestLoadAllowlist|TestAllowed' -v -count=1
```

Output shows `TestLoadAllowlistParsesAndNormalizes` with exactly 4 subtests
(`basic_two_entries`, `normalizes_trailing_slash_on_path`,
`normalizes_whitespace`, `normalizes_repeated_trailing_slash_on_path`) and
`TestLoadAllowlistRejectsInvalid` with exactly 7 subtests (`malformed_line`,
`blank_image`, `blank_path`, `blank_onlyIn`, `invalid_onlyIn`,
`bare_slash_path`, `repeated_slash_path`) — 4+11 = matches the report's
"11 allowlist scenarios split 4+7" claim exactly, all PASS.
`TestAllowed`'s pre-existing 5 subtests are unchanged (byte-identical
function, confirmed via diff).

No loop-variable capture bug: each `t.Run` closure receives `c` by value
from `for _, c := range cases` and dereferences fields synchronously inside
the closure body (no goroutine, no deferred capture past the loop), and the
tests actually execute and pass per-row with distinct names, which is
empirical proof the wiring is correct (a swapped/aliased field would show
up as a failing or misnamed subtest).

### Full-suite regression check

```
cd libs/atlas-wz && GOCACHE=/tmp/gocache-262 go build ./... && GOCACHE=/tmp/gocache-262 go test ./... -count=1
```
→ all packages `ok`, including `wz` and `wzdiff`.

```
cd services/atlas-data/atlas.com/data && GOCACHE=/tmp/gocache-262 go build ./... && GOCACHE=/tmp/gocache-262 go test ./data/wztoxml/... -count=1
```
→ `ok atlas-data/data/wztoxml` (untouched, as claimed).

`gofmt -l` on both changed test files: no output (clean).

## Disposition record (`dom20-dispositions.md`)

- Basis citation present at the top: `testing-guide.md:18` ("Prefer") and
  `task-085-packet-audit-coverage-matrix/audit-backend.md:39` precedent —
  matches the brief's requirement to cite both once.
- All 10 findings have a row; findings 5 and 6 are `CONVERTED`
  (6 marked `CONVERTED (partial)` since `TestAllowed` in the same file was
  already a table and untouched — accurately described).
- Every `KEPT FLAT` row names the specific test(s) and the rule clause
  (single scenario / sequential-stateful / narrative round-trip):
  - #1 `directory_error_test.go` — single scenario each, different
    contracts (failure vs. success). Spot-checked: plausible without
    re-reading the file in full since this finding was untouched and out of
    the mechanical-coverage focus of this review; the reasoning given
    (different contracts, not shared assertions) is internally consistent
    with the rule.
  - #8 `selfcheck_test.go` — claims `TestSelfCheckCorruptedArchiveFails` is
    "sequential/stateful" because it traces a clean archive to compute a
    corruption offset before re-checking. **Verified directly against
    source** (`wzdiff/selfcheck_test.go:66-110`): the test does build →
    `traceSubEvent` → patch the discovered offset → `SelfCheck` again — the
    corruption step genuinely depends on output from the trace step within
    the same test function. This is a correct citation, not a hand-wave.
  - #2, #3, #4, #7, #9, #10 each name the specific tests and give a reason
    that maps to "distinct contracts, not shared assertions" or "different
    assertion shapes" — read as text, these are specific enough to trace
    back to a named test without re-litigating the audit, satisfying the
    brief's "must stand on its own" requirement. Not independently
    re-verified for every one of these six line-by-line (out of the
    "coverage did not shrink" mechanical-check scope requested), since no
    code in these six files was touched — the finding is that they were
    *left alone*, which the diff stat confirms.

No `KEPT FLAT` row is a blanket "preference, not MUST" hand-wave with no
named test — every row names at least one specific `Test...` function.

## go.work.sum

`git status --porcelain | grep go.work.sum` shows ` M go.work.sum` in the
working tree, but it is **not** in `git diff --name-only 36882bf08..HEAD`
and not part of commit `ca8b7980a`. Not staged by this task, as the brief
required.

## Findings

None blocking. No coverage shrinkage found in either conversion; every
pre-existing scenario has a 1:1 post-conversion `t.Run` with identical
inputs and assertions, confirmed by diffing pre-image blobs against the
working tree and by executing `go test -v` to enumerate actual subtest
names. The two "empty `wantErrSubstr`" rows in
`TestLoadAllowlistRejectsInvalid` are not the classic silently-skipped-
assertion bug — they correctly mirror originals that never asserted an
error substring.

Non-blocking observation: dispositions #1–4, #7, #9, #10 were evaluated by
this reviewer at the level of "reasoning is internally consistent and cites
a specific test," not by independently re-deriving whether each really is a
single-scenario/sequential/narrative case from scratch — those files were
not touched in this range and the mechanical coverage check the reviewing
brief asked for concentrated on findings 5 and 6 (the two conversions,
where regression risk actually lives). If a future reviewer wants full
independent re-derivation of all 8 "KEPT FLAT" calls, that is additional
work beyond what a pure-refactor diff review requires, since none of those
8 files changed.

## Not evaluable

None. All artifacts needed for this review (pre-commit blobs via `git
show`, the working tree, and `go test -v` output) were available and used.
