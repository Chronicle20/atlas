# Review: Task 24 — FILE-05 closeout evidence (commit `9c66d7c`)

Scope: `git show 9c66d7c` — `inventory-dom01-other.txt`, `inventory-file05-builders.txt`,
`progress.md`. This is an evidence-only task (no Go code changed); build/test are not applicable.
Per the controller's brief, the headline claim (the five-line FILE-05 residue matches the five
documented dispositions) was already independently confirmed and was not re-litigated here.

## 1. Is the committed inventory what `sweep.sh` produces right now?

Ran `bash docs/tasks/task-263-backend-guideline-conformance/sweep.sh` in the worktree.

```
$ git status --short docs/tasks/task-263-backend-guideline-conformance/inventory-*.txt
(no output)
```

**PASS.** Re-running the script against the current tree produces byte-identical output to what's
committed — no diff on any of the five files the script writes
(`inventory-dom04-outbound.txt`, `inventory-dom04-inbound.txt`, `inventory-file05-builders.txt`,
`inventory-dom01-newmodelbuilder.txt`, `inventory-dom01-other.txt`). Working tree left clean aside
from the pre-existing unrelated modifications (`agent-ledger.tsv`, `go.work.sum`) that predate this
review and were not touched by me.

## 2. `inventory-dom01-other.txt` churn (39/67 rows)

```
$ git show 9c66d7c -- .../inventory-dom01-other.txt | grep -c '^-services\|^-libs'
39
$ git show 9c66d7c -- .../inventory-dom01-other.txt | grep -c '^+services\|^+libs'
39
```

Row count unchanged (67 before, 67 after — confirmed via `git show <base>:...` and
`git show 9c66d7c:...`, both `wc -l` = 67).

Built `(package-dir, declaration-text)` pairs (stripping only path-basename and line-number) for
both the before and after trees and diffed the sorted multisets — **identical**, zero difference.
This proves every changed row differs **only** in its file-path/line-number component; no
declaration text changed, and no row was added or dropped.

Also confirmed directionality: every row whose path changed now points at a `builder.go` (all 39
`>` lines have basename `builder.go`), and none of the 39 `<` (old) lines already pointed at
`builder.go` — old basenames were `connection.go`, `context.go`, `message.go`, `model.go`,
`recipients.go`, `reference_data.go`. Matches the expected effect of Tasks 19-23B moving
constructors into `builder.go`.

**PASS.**

## 3. The three "already current" inventories

`inventory-dom04-outbound.txt`, `inventory-dom04-inbound.txt`, `inventory-dom01-newmodelbuilder.txt`
show zero diff in the commit (`git diff 9c66d7c^ 9c66d7c --` on all three = empty), and the fresh
`sweep.sh` run above reproduced them byte-for-byte. These are genuinely regenerated by the script
(confirmed by reading `sweep.sh` — it writes exactly these three plus the two that did change) and
happened to match, not skipped.

**PASS**, with one accuracy note (non-blocking, see Finding 1 below): `sweep.sh` writes exactly
**five** files. `progress.md` and the implementer report both fold in additional stale files
(`inventory-dom01-modelbuilder-type.txt`, `inventory-dom04-has-model.txt`,
`inventory-dom04-no-model.txt`) as if they were part of the "already current, no diff" set produced
by this sweep. They are not written by `sweep.sh` at all (confirmed by reading the script body,
which only ever opens/writes `inventory-dom04-outbound.txt`, `inventory-dom04-inbound.txt`,
`inventory-file05-builders.txt`, `inventory-dom01-newmodelbuilder.txt`,
`inventory-dom01-other.txt`). `git log` on those three stale files shows they were last touched by
earlier, unrelated tasks (Task 9a's FR-13 scope-gap commit, the DOM-04 closeout commit, and the
Tasks 19-21 commit) — not by any run of the current `sweep.sh`. They appear in the end-of-script
`wc -l "$OUT"/inventory-*.txt` only because that line globs the whole directory, not because the
script regenerates them.

## 4. `sweep.sh` unmodified

```
$ git log --oneline -- docs/tasks/task-263-backend-guideline-conformance/sweep.sh
b74006ae3 spec(task-263): initial PRD for backend-guideline-conformance
```

Only one commit in the branch's history has ever touched `sweep.sh` — the initial PRD commit. Task
24's commit (`9c66d7c`) does not touch it, and no other commit on this branch has modified it since.
**PASS.**

## 5. `progress.md` prose accuracy

The `## Task 24 — FILE-05 closeout` section (lines 3656-3696) pastes the verbatim five-line residue,
which matches byte-for-byte what I reproduced by re-running the script (Finding item 1 above), and
correctly states the `inventory-dom01-other.txt` 39/67 churn with the same "39 removed / 39 added,
no row lost or gained" framing I independently verified.

**Non-blocking finding:** the sentence "only two of the five changed against the pre-Task-24 tree,
because they were last regenerated before Tasks 19-23-B landed (`inventory-dom04-outbound.txt`,
`inventory-dom04-inbound.txt`, and `inventory-dom01-modelbuilder-type.txt`/
`inventory-dom01-newmodelbuilder.txt` were already current and printed no diff)"
(`docs/tasks/task-263-backend-guideline-conformance/progress.md:3663-3665`) folds a file
`sweep.sh` never writes (`inventory-dom01-modelbuilder-type.txt`) into the same clause as the actual
sweep-regenerated `inventory-dom01-newmodelbuilder.txt`, joined by a slash as though they were
interchangeable names for the same artifact. They are two distinct, unrelated files. This
overstates what the sweep re-run actually confirmed for `inventory-dom01-modelbuilder-type.txt` (it
was never touched by `sweep.sh` in this run or the prior one — the "already current" framing implies
it was checked-and-matched, when in fact it's simply outside this script's write set). It does not
affect the FILE-05 headline claim, which is independently correct, but it is a factual imprecision
in an evidence-recording task whose entire purpose is precise record-keeping.

## Findings

**Non-blocking (1):**
- `docs/tasks/task-263-backend-guideline-conformance/progress.md:3663-3665` — the "already current"
  parenthetical conflates the stale, sweep.sh-untouched `inventory-dom01-modelbuilder-type.txt` with
  the actually-regenerated `inventory-dom01-newmodelbuilder.txt`, implying a file outside the
  script's write set was verified as unchanged by this sweep run. Does not affect the FILE-05
  disposition conclusion, which is independently correct.

**Not evaluable:** none. All five claims in the reviewer brief were directly checkable within the
diff plus a `sweep.sh` re-run.

## Verdict

APPROVED_WITH_FINDINGS — the substantive claims (FILE-05 residue is exactly the five documented
dispositions, DOM-01-other churn is a pure path relocation, the three other inventories are
genuinely unchanged, `sweep.sh` itself is untouched) all hold up under direct verification. The one
finding is a documentation-precision issue in `progress.md`'s prose, not a defect in the evidence
itself.
