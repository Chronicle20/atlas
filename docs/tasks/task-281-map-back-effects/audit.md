# Plan Audit — task-281-map-back-effects

**Plan Path:** docs/tasks/task-281-map-back-effects/plan.md
**Audit Date:** 2026-08-28
**Branch:** task-281-map-back-effects
**Base Branch:** main
**HEAD at audit time:** `797d1e0cf` (one commit ahead of the `8ec297163` HEAD named in the dispatch — `docs(task-281): add packet coverage manifest`, landed mid-audit and covered below)

## Executive Summary

All 13 plan tasks were implemented, matching the plan's file lists, interfaces, and test tables almost exactly. Every module-local `go build ./...` and `go test ./... -count=1` run clean (`libs/atlas-packet`, `libs/atlas-saga`, `services/atlas-maps`, `services/atlas-channel`, `services/atlas-saga-orchestrator`, `services/atlas-messages`), and all three `packet-audit --check` gates exit 0. The five approved controller rulings were carried out as specified. One additional, well-evidenced deviation not on the pre-approved list — replacing two mis-identified `SET_MAP_OBJECT_VISIBLE` registry entries on gms_v72/v79 with the correctly-derived `CLEAR_BACK_EFFECT` — is documented and justified in commit `dffb2a82c` and does not weaken the matrix. Per the session's explicit ruling, Task 13 Steps 3 (`tools/verify.sh`) and 6 (code review + PR) are correctly left pending; this audit did not run either. A `packet-completeness-critic` blocking finding (missing `coverage-manifest.yaml`) was raised and then resolved by a commit that landed after this audit was dispatched (`797d1e0cf`); it is now present and no longer a gap. `plan.md`'s own checkboxes were never ticked despite the work being committed — a cosmetic tracking gap only.

## Task Completion

| # | Task | Status | Evidence / Notes |
|---|------|--------|------------------|
| 1 | Derive GMS wire layout on v79/83/87/92 (+v72/v84 transcription) | DONE | `structures/gms_{72,79,83,84,87,92}.md` all present with Router/read-order/Verdict sections; commit `b9bd002d1`. Layout found IDENTICAL on all six, no `MajorAtLeast` gate written — matches approved ruling #2. |
| 2 | Derive jms_v185; prove ⬜ cells absent | DONE (with in-task correction, approved ruling #4) | `structures/{gms_v95,jms_v185,gms_v48,gms_v61}.md` present; commit `e45a6ed34`. gms_v61 derivation disproved the absence premise (both ops present, opcodes 95/96) — commit message states this explicitly; only gms_v48 ended up VERSION-ABSENT. `absence-clear-v72-v79.md` (Step 3) was never written because `gms_v72.md`/`gms_v79.md` (Task 1) directly proved `CLEAR_BACK_EFFECT` is PRESENT on both, with an explicit `FINDING:` note deferring the registry-gap disposition to Task 2/4 — correctly resolved by promoting those cells in Task 4 instead of writing an absence file for an op that isn't absent. Commit `265d9668b` corrects earlier v61/jms evidence. |
| 3 | Two clientbound codecs | DONE | `libs/atlas-packet/field/clientbound/set_back_effect.go`, `clear_back_effect.go`, and both `_test.go` files match the plan's struct/interface spec verbatim (`set_back_effect.go:14-70`, `clear_back_effect.go:13-38`). Golden + round-trip tests present; `go test ./field/clientbound/ -run BackEffect -v` → all PASS, including an extra `gms_v61` variant/marker beyond the plan's original 8 (justified by Task 2's finding that v61 is present, not absent). Commit `a9cc197bb`. |
| 3b | Name BackEffect handler fnames in IDA exports (extra unit, user-approved) | DONE | Commit `4f87fdfb4`, per approved ruling #3; spliced v61/v72/v79/v92 export fnames. |
| 4 | Registry links, seed-template routes, evidence, matrix promotion | DONE | Commit `dffb2a82c`. `docs/packets/audits/STATUS.md:187,190` show 18 ✅ cells and 1 ⬜ (gms_v48) per row, matching the corrected 1-absent-version reality, not the plan's original 6-blank-cell estimate. `feature-na-evidence.yaml` carries exactly the two gms_v48 entries (approved ruling #4). `go run ./tools/packet-audit matrix --check`, `fname-doc --check`, `operations --check` all exit 0 (verified live). One additional, evidenced deviation beyond the pre-approved list: the commit message documents replacing DISPROVEN `SET_MAP_OBJECT_VISIBLE` registry entries at gms_v72 opcode 118 / gms_v79 opcode 122 with `CLEAR_BACK_EFFECT`, because both addresses read zero packet bytes and cannot be `OnSetMapObjectVisible`. This is grounded in Task 1's own decompile of those exact thunks and is recorded as a follow-up (`follow-ups.md`) rather than silently landed. |
| 5 | `atlas-maps` back-effect registry and processor | DONE | `map/backeffect/registry.go`, `processor.go`, `registry_test.go` match the plan's `FieldKey`/`BackEffectEntry`/`Processor` interfaces exactly (`registry.go:9-30`, `processor.go:13-25`). All 4 named tests present and pass (`TestSetThenGetActive`, `TestSetReplacesSamePageInPlace`, `TestClearRemovesEveryPage`, `TestBackEffectIsTenantIsolated`). Commit `827d1592b`. |
| 6 | `atlas-maps` command consumer / status-event producer | DONE | `kafka/message/map/command.go` and `kafka.go` gain the constants/structs; `map/backeffect/producer.go` new; `kafka/consumer/map/consumer.go:110-162` implements `handleSetBackEffectCommand`/`handleClearBackEffectCommand` exactly per the plan's pseudocode, including the invalid-effect warn-and-reject and the "produce event anyway on empty clear" behavior (`consumer.go:144-160`). All 5 named tests present and pass. Commit `372af1f83`. |
| 7 | `atlas-maps` back-effect REST collection | DONE | `map/backeffect/{rest,resource,resource_test}.go` match plan; `main.go:15,151` registers `backeffect.InitResource`. `res := make([]RestModel, 0, len(entries))` empty-is-`[]` behavior present (`resource.go:38`). Commit `a84d1587e`, follow-up fix `62b7d981b` (error-check on `Body.Close` in the test, a hygiene fix not a scope change). |
| 8 | `atlas-channel` writers and status-event broadcast | DONE | `kafka/message/map/kafka.go` mirrors atlas-maps byte-for-byte (confirmed below); `socket/writer/{set,clear}_back_effect.go` added; `main.go` writer-name slice updated; `consumer.go:1168-1206` implements both status-event handlers per the plan's pseudocode. All 3 named tests present and pass (`TestHandleStatusEventBackEffectSet_BroadcastsToField`, `_IgnoresOtherChannel`, `TestHandleStatusEventBackEffectClear_BroadcastsToField`). Commit `057060bee`. |
| 9 | `atlas-channel` REST client and map-entry replay | DONE | `backeffect/{rest,requests,processor,mock/processor}.go` added; `announceActiveBackEffects` (`consumer.go:1219-1231`) matches the plan's code block verbatim, including the `Duration: 0` replay and fail-open `if err != nil { return }`. Dispatched from its own `routine.Go` block per plan (not folded into `announceActiveVisuals`) — confirmed at `consumer.go:397`. All 3 named tests present and pass. Commit `be28fe948`. |
| 10 | `libs/atlas-saga` actions and payloads | DONE | `model.go:291-296`, `payloads.go:1322-1341`, `unmarshal.go:648-655` add the two actions/payloads/decode arms exactly as specified. Both named tests (`TestUnmarshalSetBackEffectStep`, `TestUnmarshalClearBackEffectStep`) present and pass. Commit `bae72f163`. |
| 11 | `atlas-saga-orchestrator` handlers and map commands | DONE | `saga/model.go`, `saga/handler.go`, `saga/event_acceptance.go`, `kafka/message/map/kafka.go`, `map_command/{processor,producer,producer_test}.go` all updated. All 4 named tests present and pass (`TestHandleSetBackEffect_InvalidPayload`, `TestHandleClearBackEffect_InvalidPayload`, `TestSetBackEffectCommandProvider`, `TestClearBackEffectCommandProvider`). Commit `b11dfdf67`. |
| 12 | `atlas-messages` GM chat commands | DONE | `command/map/back_effect.go` implements `BackEffectCommandProducer`/`ClearBackEffectCommandProducer` per plan, including the `[01]`-constrained regex, `c.Gm()` gate, and `FieldId: uint32(f.MapId())` fill-in. `kafka/message/map/kafka.go` gains the command types (used `kafka.go`, not a `command.go` — matches approved ruling #5, since this service has no `command.go`). `main.go` registers both producers. All 10 table-driven subtests present and pass. Commit `3e0251dda`. |
| 13 | Full verification gate | PARTIAL by design (approved ruling #6) | Step 1 (follow-ups.md) done: commit `8ec297163` documents `SET_MAP_OBJECT_VISIBLE` as the next-cheapest follow-up, citing per-version router addresses. Step 2 (packet-audit `--check` gates) verified live in this audit: `matrix --check`, `fname-doc --check`, `operations --check` all exit 0. Step 4 (cross-service seam trace) verified live: `BACK_EFFECT_SET`/`BACK_EFFECT_CLEAR` constants and `BackEffectSet`/`BackEffectClear` structs are byte-identical between `services/atlas-maps/.../kafka/message/map/kafka.go` and `services/atlas-channel/.../kafka/message/map/kafka.go`. Step 5 (commit) done. Steps 3 (`tools/verify.sh`) and 6 (code review + PR) are correctly **not yet run/opened** per this session's explicit ruling; not evaluated here per instruction. |

**Completion Rate:** 13/13 plan tasks completed (100%); Task 13 has two steps intentionally deferred per controller ruling, not counted as incomplete.
**Skipped without approval:** 0
**Partial implementations:** 1 (Task 13, by design/approved ruling — not a defect)

## Skipped / Deferred Tasks

None skipped without approval. Task 13's Steps 3 and 6 are deliberately pending (flagless `tools/verify.sh` and code review + PR are running/queued outside this audit's scope, per the dispatch instructions). No impact assessment needed beyond: this branch cannot be called merge-ready until those two steps report green, independent of this audit's findings.

One item worth flagging even though it resolved itself during the audit: a `packet-completeness-critic` run (`docs/tasks/task-281-map-back-effects/completeness-critic.md`) found a BLOCKING gap — no `coverage-manifest.yaml` existed for this packet task, which the process (`docs/packets/PROCESS.md` §"Coverage manifest") requires for every packet task. That gap was closed by commit `797d1e0cf` (`docs(task-281): add packet coverage manifest`), which landed on the branch after this audit was dispatched. The manifest now exists, declares the same op/version/out-of-scope set that the actual STATUS.md/status.json reflect, and is internally consistent with the structures records. No outstanding action needed, but note that HEAD moved from the `8ec297163` cited in the dispatch to `797d1e0cf` during this audit.

## Build & Test Results

| Service / Module | Build | Tests | Notes |
|---------|-------|-------|-------|
| `libs/atlas-packet` | PASS | PASS | `go test ./field/clientbound/ -run BackEffect -v` — all golden + round-trip subtests pass across 12 version variants (incl. gms_v61). |
| `libs/atlas-saga` | PASS | PASS | `go test ./... -run BackEffect -v` — 2/2 pass. |
| `services/atlas-maps/atlas.com/maps` | PASS | PASS | `go test ./... -count=1` — all packages ok, including `map/backeffect` (0.058s). No FAIL lines. |
| `services/atlas-channel/atlas.com/channel` | PASS | PASS | `go test ./... -count=1` — all packages ok, no FAIL lines; `backeffect`/`backeffect/mock` report "no test files" (expected — pure client wrapper packages, exercised via `kafka/consumer/map` tests per plan). |
| `services/atlas-saga-orchestrator/atlas.com/saga-orchestrator` | PASS | PASS | `go test ./... -count=1` — all packages ok, `saga` and `map_command` include the new BackEffect tests, all pass. |
| `services/atlas-messages/atlas.com/messages` | PASS | PASS | `go test ./... -count=1` — no FAIL lines; `command/map` BackEffect table-driven tests (10 subtests) all pass. |

Note: `tools/verify.sh` and `tools/lint.sh` were intentionally NOT run in this audit per explicit instruction (a repo-wide gate was already running in the background). Module-local build/test only, as instructed.

## Overall Assessment

- **Plan Adherence:** FULL
- **Recommendation:** NEEDS_REVIEW — implementation and module-level verification are complete and clean, but the branch is explicitly mid-flight on the two gating steps (`tools/verify.sh` flagless run, code review + PR) that this audit was told not to perform. Do not treat this audit as a substitute for those.

## Action Items

1. Complete Task 13 Step 3: run the flagless `tools/verify.sh` to completion and confirm exit 0 (currently running in background per the dispatch note; re-check its result before merge).
2. Complete Task 13 Step 6: dispatch `backend-guidelines-reviewer` over the changed Go packages and `packet-completeness-critic` over the packet-layer scope, then open the PR. Note the completeness-critic's earlier BLOCKING finding (missing coverage-manifest.yaml) is already resolved by commit `797d1e0cf`; a re-run should now proceed past Step 1 and execute its diff steps against the manifest.
3. Cosmetic only: none of `plan.md`'s 78 checkbox items are ticked (`- [x]`) despite every task being committed and verified. Not a functional gap, but future readers of `plan.md` alone would not see task-by-task completion status without cross-referencing git log. Consider updating the checkboxes for the historical record, at the implementer's/controller's discretion.
