# Review: Task 4 — Registry links, seed-template routes, evidence, matrix promotion

Range: `4f87fdfb4..dffb2a82c` (single commit `dffb2a82c`, 46 files)
Brief: `.superpowers/sdd/plan/task-4-brief.md` (task text + two controller addenda)
Implementer report: `.superpowers/sdd/plan/task-4-report.md` (DONE_WITH_CONCERNS)

## Scope confirmed

`git diff --stat` matches the 46-file set the report claims: 9 registries, 5 spliced
ida-exports, 18 new evidence records, `feature-families.yaml`, `feature-na-evidence.yaml`,
`STATUS.md`/`status.json` (regenerated), 9 seed templates, and
`socket/corpus_test.go`. A negated pathspec diff (`-- . ':!docs/packets'
':!services/atlas-configurations/seed-data' ':!.../corpus_test.go'`) returned empty —
nothing landed outside the authorized surface.

## 1. The v72/v79 remove-and-replace (Addendum 1, ruling 2/3)

Confirmed by direct diff of both files:

- `gms_v72.yaml:844` — the old `SET_MAP_OBJECT_VISIBLE` / `fname: sub_5F5F54` entry at
  opcode 118 is **removed** (not left alongside), and `CLEAR_BACK_EFFECT` /
  `fname: CMapLoadable::OnClearBackEffect` / `packet: field/clientbound/FieldClearBackEffect`
  is added at the same opcode/address (6250324 = `0x5f5f54`), with a note stating the
  prior attribution is disproven (zero-read handler) — matches the ruling's exact wording
  and address.
- `gms_v79.yaml:865` — same pattern at opcode 122 / address 6375799 (`0x614977`).
- `status.json` (auto-regenerated) now correctly grades `SET_MAP_OBJECT_VISIBLE` as
  `state: n-a` for `gms_v72` and `gms_v79` (was `incomplete`/`❌` before). `STATUS.md`'s
  `SET_MAP_OBJECT_VISIBLE` row now shows `⬜` for those two columns, `❌` for the rest —
  exactly the state the ruling intends.
- No orphan reference to the removed `sub_5F5F54`/`sub_614977` `SET_MAP_OBJECT_VISIBLE`
  attribution survives in any registry, evidence, or template file (grep across `docs/`
  and `services/`). Historical documents (`structures/`, `task-113` deltas, `discover_*.md`,
  `review-task-*.md`) still cite the old names, which is correct — those are dated records
  of what was believed at the time, not live registry claims.
- `docs/packets/audits/support/gms_v72.md` and `gms_v79.md` (a separate, older report
  generated by a different command, `support_summary`, last touched in a #1459 commit
  unrelated to this task) still show the disproven `SET_MAP_OBJECT_VISIBLE` at opcode
  118/122 marked `incomplete | no audit report`. This is pre-existing staleness — the
  brief's Step 5 only names `matrix`, `fname-doc --check`, `operations --check`, none of
  which touch `support/*.md` — so it is not a defect introduced by this task. Flagged as
  **non-blocking**, since a reader consulting that specific file would still see the
  disproven claim.

**gms_v61 rename (Addendum 2):** confirmed a pure rename, not an add. `IDA_0X05F`
(opcode 95, address 5931798) → `SET_BACK_EFFECT`, `IDA_0X060` (opcode 96, address
5932827) → `CLEAR_BACK_EFFECT`. Addresses unchanged; only `op`, `fname`, `packet`, and
`note` values changed, matching the ruling's explicit address citations
(`0x5A8316`/`0x5A871B`). No orphan `op: IDA_0X05F`/`IDA_0X060` entry survives in any
registry (the only remaining textual occurrences of those strings are inside the new
note's own provenance sentence, e.g. `gms_v61.yaml:656` "Was the IDA_0X05F placeholder"
— correct, not a live entry — and the same pre-existing, out-of-scope `support/gms_v61.md`).

Each of the nine touched registries has exactly one `SET_BACK_EFFECT` and one
`CLEAR_BACK_EFFECT` entry (verified with `grep -c "^- op: SET_BACK_EFFECT$"` /
`CLEAR_BACK_EFFECT` across all nine files — 1/1 each). **PASS.**

## 2. Lossy-splice concern and its blast radius

Verified the report's claim directly, file by file, for the five restored exports:

```
git diff 4f87fdfb4..dffb2a82c -- docs/packets/ida-exports/{gms_v83,gms_v84,gms_v87,gms_v95,gms_jms_185}.json
```

Every one of the five diffs is a pure, minimal insertion — exactly one new
`"CMapLoadable::OnSetBackEffect": {...}` and one `"CMapLoadable::OnClearBackEffect": {...}`
block per file, both with `"direction": ""` and `"calls": null` (as Task 3b's addendum
warns is expected/non-blocking). No line is removed, no key elsewhere in any of the five
files is reordered, and no existing comment or `size` field is touched — the report's
"zero removed, zero changed" claim holds. Addresses match the report's cited Task-3
`packet-audit:verify` markers exactly: v83 `0x6445c5`/`0x6449ca`, v84 `0x659e3c`/`0x65a241`,
v87 `0x67dcdb`/`0x67e0e0`, v95 `0x612850`/`0x61f230`, jms185 `0x6ba27f`/`0x6ba684`.
**PASS.**

The four Task 3b exports (`gms_v61/72/79/92.json`) show **zero** diff in this commit
(`git diff --stat -- docs/packets/ida-exports/` lists only the five files above) — this
task did not touch them, consistent with the instruction not to re-litigate the
controller's prior clearance of those four.

The underlying tool defect (`idasrc.SpliceExport` round-tripping through the struct,
dropping `DecodeBuffer.size`, re-sorting keys, HTML-escaping `->`) is real and worth a
follow-up, but it was correctly worked around here and is out of this task's scope to fix.

## 3. Registry/template/matrix consistency

Ran the three check commands myself from repo root:

```
go run ./tools/packet-audit matrix --check    → exit 0
go run ./tools/packet-audit fname-doc --check → "fname-doc check OK (273 structs without an audit report carry no fname)", exit 0
go run ./tools/packet-audit operations --check → "operations check OK (0 absent-writer note(s))", exit 0
```

```
grep -E '^\| (SET|CLEAR)_BACK_EFFECT ' docs/packets/audits/STATUS.md
```
returns 18 `✅` cells and 2 `⬜` cells (both gms_v48) across the two rows — exactly the
expected Addendum-2 shape.

Re-ran `go run ./tools/packet-audit matrix` (the non-`--check` regeneration form) and
confirmed **zero diff** against the committed `STATUS.md`/`status.json` — the committed
artifacts are exactly the tool's own output, not hand-tweaked.

All nine templates (`gms_61/72/79/83/84/87/92/95` + `jms_185`) route both `SetBackEffect`
and `ClearBackEffect` with `services: ["channel"]`, inserted at the correct opcode per the
per-version table in the report, and each insertion is ascending-opCode-order relative to
its neighbours (spot-checked all nine diffs). `template_gms_61_1.json` — the file the plan
text omitted and Addendum 2 added back — is present and correctly routes both writers.
`gms_v48.yaml` and any `gms_v12` registry are untouched (`gms_v12.yaml` does not exist,
consistent with the report). **PASS.**

Registry diffs for `gms_v83/84/87/92/95` and `jms_v185` are minimal — exactly one
`packet:` line added per SET/CLEAR entry, no other field (`opcode`, `fname`, `ida`,
`provenance`, `note`) touched, including `gms_v92`'s pre-existing stale
"Not IDA-confirmed by the gms_v92 discover-ops pass (task-26)" note, left untouched as
the report describes (non-blocking, self-flagged, defensible — the statement about
task-26's pass remains historically true).

## 4. Accepted deviations

**(a) gms_v48 VERSION-ABSENT records.** Read `tools/packet-audit/cmd/na_consistency.go`
directly: its header comment states the n-a-consistency gate is check-only and reads
`docs/packets/feature-na-evidence.yaml` as the positive-absence proof for a sibling-verified
family — precisely the mechanism the report describes and the two `gms_v48` entries use.
The two entries added to `feature-na-evidence.yaml` cite the same router enumeration and
`ReloadBack`-xref-count reasoning as `structures/gms_v48.md`, are not paraphrased beyond
what that source supports, and `matrix --check`'s only two console notes are for unrelated
pre-existing families (`CASHSHOP_CASH_ITEM_GACHAPON_RESULT × gms_v79`,
`USE_TELEPORT_ROCK × gms_v48`) — no back_effect-related warning or error. Judged as the
correct mechanism and a defensible, well-evidenced deviation from the brief's Step-3 text.
**Not a defect.**

**(b) Corpus-size guard 3387 → 3405.** `TestValidate_AcceptsEverySeedTemplate` computes
`total` by summing parsed `handlers`+`writers` across all 11 seed templates at test time —
it is not a hardcoded independent literal that happens to match. 9 templates × 2 new
writer bindings = 18, and 3387 + 18 = 3405 exactly. Ran the test directly:
`go test ./socket/... -run TestValidate_AcceptsEverySeedTemplate -v` → `PASS`. The `if
len(files) != 11` guard also matches the actual template count
(`ls .../templates/template_*.json | wc -l` → 11). **Not fitted; genuine.**

## 5. Standard scope check

No files outside `docs/packets/`, `services/atlas-configurations/seed-data/templates/`,
and the one `corpus_test.go` guard update are touched. File count and per-file diff sizes
match the report's own accounting.

## Not evaluable

- Whether the newly-declared `packet:` back-effect struct decode logic in
  `libs/atlas-packet/field/clientbound/{set,clear}_back_effect.go` is itself correct is out
  of this task's surface — those files pre-exist (from an earlier task in the series) and
  are unmodified here.
- Whether a Go writer function literally named `SetBackEffect`/`ClearBackEffect` exists and
  is wired into `atlas-channel`'s dispatch table was not checked — `corpus_test.go`'s
  `Validate` only checks structural template rules (opcode format, duplicate detection,
  service validity), not that a matching Go symbol exists, so this task's own test surface
  does not exercise that binding. This is very likely a downstream task's concern, but I
  did not trace it, since Task 4's brief and diff are scoped to docs/registry/template
  wiring only.

## Verdict rationale

Every priority-ordered check the controller asked for came back confirmed: the v72/v79
remove-and-replace is real and orphan-free (with one flagged pre-existing stale-doc note),
the five restored splice exports are byte-clean insertions, all three `--check` gates pass
with the expected 18✅/2⬜ shape, the na-evidence mechanism is the one the gate actually
reads, and the corpus-guard number is genuine. The implementer's self-reported concerns are
accurate and appropriately scoped (tooling defect noted for follow-up, not silently
worked around; deviation (a) explained and judged correct here). One new, non-blocking
finding: `docs/packets/audits/support/gms_v72.md` and `gms_v79.md` (out of this task's
regen scope) still show the disproven `SET_MAP_OBJECT_VISIBLE` attribution.
