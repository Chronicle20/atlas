# Review: c269d403d — "docs(git-workflow): require a live smoke test section in every PR body"

Branch: `docs/pr-body-live-smoke-test`
Scope: docs-only, 2 files (`CLAUDE.md`, `docs/git-workflow.md`), reviewed against repo source per the review brief.

## Claims verified

1. **`<N>.atlas.home` host convention** — confirmed real, used identically in
   `services/atlas-pr-bootstrap/scripts/sweep-orphans.sh:383`,
   `services/atlas-pr-bootstrap/scripts/cleanup.sh:493`, and throughout
   `docs/runbooks/ephemeral-pr-deployments.md`.

2. **Port formula** — `loginPort = majorVersion × 100`, `channelPort = loginPort + 1`.
   Confirmed verbatim in `services/atlas-pr-bootstrap/scripts/version-ports.sh:2-3`:
   ```
   #   loginPort(major)   = major * 100
   #   channelPort(major) = loginPort(major) + 1
   ```
   and the file's own header states it is the sole source of port arithmetic
   in the repo ("No port arithmetic is written anywhere else in the repo").
   The doc's citation of this file as the owning file is correct.

3. **`deploy-env` label gates the environment's existence** — confirmed at
   `docs/runbooks/ephemeral-pr-deployments.md:563`: "ApplicationSet only
   generates an Application for PRs carrying the `deploy-env` label." Also
   corroborated by `.github/workflows/pr-validation.yml:784` (`HAS_LABEL`
   check) and `pr-cleanup.yml:50` (teardown on label removal).

4. **Sparse mode serves unchanged services from `main`; `atlas:isolated` is the correct escalation label** — confirmed at
   `docs/runbooks/ephemeral-pr-deployments.md:832-835` (sparse: "everything
   else in the namespace is served by `main`") and `:871`
   (`gh pr edit <N> --add-label atlas:isolated` — exact command, exact label).

5. **`.github/workflows/pr-env-smoke.yml` exists and proves basic env
   bring-up/teardown** — file exists (9.4K). Its header comment
   (`pr-env-smoke.yml:1-11`) states it "Opens + closes a synthetic PR labeled
   `deploy-env`; asserts every per-env artifact is reclaimed within the
   timeout." This supports the doc's "don't repeat it" rationale for basic
   env-came-up/teardown checks.

6. **Runbook section §9.15** — confirmed to exist and be exactly on-topic:
   `docs/runbooks/ephemeral-pr-deployments.md:827` is titled "§9.15 Sparse vs.
   isolated mode, and the per-PR override labels" — the section number and
   content match the doc's citation.

7. **Worked example arithmetic** — `v83 → login 8300, channel 8301`:
   83 × 100 = 8300, 8300 + 1 = 8301. Matches formula exactly, and is
   independently corroborated by
   `docs/tasks/task-063-ephemeral-pr-deployments/design.md:295` and
   `plan.md:2809`, both stating atlas-login wire port 8300 / atlas-channel
   wire port 8301 for the same PR-env socket setup.

8. **`ENVIRONMENT: pr-<N>` header** — name and value shape confirmed as the
   real repo convention for scoping API calls to a PR environment, corroborated
   by `docs/tasks/fix-provisioning-env-self-writes/brief.md:10`
   (`-H "ENVIRONMENT: pr-1411"`), `docs/tasks/task-242-sparse-tenant-provisioning/design.md:347,551`,
   and `plan.md:158-428` (multiple `env_header_init`/curl-arg assertions all
   using this exact header name/value shape).

## Repo conventions

- No absolute/home paths introduced in the new section (`grep` for
  `/home`, `/Users`, `C:\` in the commit diff returned no matches).
- No CRLF introduced; the diff has 0 lines matching `^M$`.
- All file references are repo-relative
  (`services/atlas-pr-bootstrap/scripts/version-ports.sh`,
  `.github/workflows/pr-env-smoke.yml`).
- The `## Live smoke test` string at `docs/git-workflow.md:70` is inside a
  fenced ```markdown``` example block illustrating PR-body shape, not a
  real second top-level heading — no duplicate-heading or doc-structure
  issue.

## CLAUDE.md table edit

`CLAUDE.md:75` — only the git-workflow row's trigger text changed
(`"Committing, pushing, rebasing"` → `"Committing, pushing, rebasing, writing
a PR body"`); the owner link, table delimiters, and all other rows are
untouched. Confirmed via `git show c269d403d -- CLAUDE.md`.

## Not evaluable

None. Every factual claim in the new section was traceable to a specific
`file:line` in repo source within the review's scope.

## Verdict

All claims in the new "PR body: the live smoke test" section, and the
CLAUDE.md trigger-table edit, are factually correct against repo source. No
invented values, opcodes, hosts, ports, labels, or paths were found.
