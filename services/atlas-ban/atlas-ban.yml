---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-ban
  namespace: atlas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-ban
  template:
    metadata:
      labels:
        app: atlas-ban
    spec:
      containers:
      - name: ban
        image: ghcr.io/chronicle20/atlas-ban/atlas-ban:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
        - name: DB_NAME
          value: "atlas-ban"
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-ban
  namespace: atlas
spec:
  selector:
    app: atlas-ban
  ports:
  - protocol: TCP
    port: 8080
