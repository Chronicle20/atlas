---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-buddies
  namespace: atlas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-buddies
  template:
    metadata:
      labels:
        app: atlas-buddies
    spec:
      containers:
      - name: buddies
        image: ghcr.io/chronicle20/atlas-buddies/atlas-buddies:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-buddies"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-buddies
  namespace: atlas
spec:
  selector:
    app: atlas-buddies
  ports:
  - protocol: TCP
    port: 8080