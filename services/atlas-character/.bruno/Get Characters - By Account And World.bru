meta {
  name: Get Characters - By Account And World
  type: http
  seq: 1
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/characters?accountId={{accountId}}&worldId={{worldId}}
  body: none
  auth: none
}

params:query {
  accountId: {{accountId}}
  worldId: {{worldId}}
}
