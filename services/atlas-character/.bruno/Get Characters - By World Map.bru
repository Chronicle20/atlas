meta {
  name: Get Characters - By World Map
  type: http
  seq: 2
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/characters?worldId={{worldId}}&mapId={{mapId}}
  body: none
  auth: none
}

params:query {
  worldId: {{worldId}}
  mapId: {{mapId}}
}
