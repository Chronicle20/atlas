meta {
  name: Get Item By Inventory Slot
  type: http
  seq: 6
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/characters/{{characterId}}/inventories/{{inventoryId}}/items?slot={{slot}}
  body: none
  auth: none
}

params:query {
  slot: {{slot}}
}
