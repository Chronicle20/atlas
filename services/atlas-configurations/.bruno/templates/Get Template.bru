meta {
  name: Get Template
  type: http
  seq: 2
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/configurations/templates?region={{REGION}}&majorVersion={{MAJOR_VERSION}}&minorVersion={{MINOR_VERSION}}
  body: none
  auth: none
}

params:query {
  region: {{REGION}}
  majorVersion: {{MAJOR_VERSION}}
  minorVersion: {{MINOR_VERSION}}
}
