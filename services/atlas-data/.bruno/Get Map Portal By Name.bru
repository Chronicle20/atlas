meta {
  name: Get Map Portal By Name
  type: http
  seq: 8
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/data/maps/{{mapId}}/portals?name={{portalName}}
  body: none
  auth: none
}

params:query {
  name: {{portalName}}
}
