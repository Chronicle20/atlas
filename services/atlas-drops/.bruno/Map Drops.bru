meta {
  name: Map Drops
  type: http
  seq: 2
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/worlds/{{worldId}}/channels/{{channelId}}/maps/{{mapId}}/instances/{{instanceId}}drops
  body: none
  auth: none
}
