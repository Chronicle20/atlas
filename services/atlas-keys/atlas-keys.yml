---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-keys
  namespace: atlas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: atlas-keys
  template:
    metadata:
      labels:
        app: atlas-keys
    spec:
      containers:
      - name: keys
        image: ghcr.io/chronicle20/atlas-keys/atlas-keys:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-keys"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-keys
  namespace: atlas
spec:
  selector:
    app: atlas-keys
  ports:
  - protocol: TCP
    port: 8080