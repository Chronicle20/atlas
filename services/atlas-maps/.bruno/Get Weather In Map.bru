meta {
  name: Get Weather In Map
  type: http
  seq: 3
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/worlds/{{worldId}}/channels/{{channelId}}/maps/{{mapId}}/instances/{{instanceId}}/weather
  body: none
  auth: none
}
