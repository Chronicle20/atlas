---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-notes
  namespace: atlas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-notes
  template:
    metadata:
      labels:
        app: atlas-notes
    spec:
      containers:
      - name: notes
        image: ghcr.io/chronicle20/atlas-notes/atlas-notes:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-notes"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-notes
  namespace: atlas
spec:
  selector:
    app: atlas-notes
  ports:
  - protocol: TCP
    port: 8080