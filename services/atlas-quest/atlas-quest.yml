---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-quest
  namespace: atlas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-quest
  template:
    metadata:
      labels:
        app: atlas-quest
    spec:
      containers:
      - name: quest
        image: ghcr.io/chronicle20/atlas-quest/atlas-quest:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: atlas-env
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_NAME
          value: "atlas-quest"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: atlas-quest
  namespace: atlas
spec:
  selector:
    app: atlas-quest
  ports:
  - protocol: TCP
    port: 8080
