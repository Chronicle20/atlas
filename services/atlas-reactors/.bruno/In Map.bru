meta {
  name: In Map
  type: http
  seq: 5
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/worlds/{{worldId}}/channels/{{channelId}}/maps/{{mapId}}/instances/{{instanceId}}/reactors
  body: none
  auth: none
}
