meta {
  name: Get Storage
  type: http
  seq: 5
}

get {
  url: {{scheme}}://{{host}}:{{port}}/api/storage/accounts/{{accountId}}?worldId={{worldId}}
  body: none
  auth: inherit
}

params:query {
  worldId: {{worldId}}
}
