meta {
  name: Delete Tenant Configuration Instance Route
  type: http
  seq: 3
}

delete {
  url: {{scheme}}://{{host}}:{{port}}/api/tenants/{{tenantId}}/configurations/instance-routes/{{routeId}}
  body: json
  auth: inherit
}
