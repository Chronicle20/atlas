meta {
  name: Seed Tenant Configuration Routes
  type: http
  seq: 5
}

post {
  url: {{scheme}}://{{host}}:{{port}}/api/tenants/{{tenantId}}/configurations/routes/seed
  body: json
  auth: inherit
}
